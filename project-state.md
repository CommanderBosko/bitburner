# Project State

_Last updated: 2026-07-26_

## Current Project State

> **BitNode 1 was completed and the save reset fresh into BitNode 4** sometime between the 2026-07-24 close (BN1, $34T net worth, chasing the Daedalus/Red Pill/`w0r1d_d43m0n` win path) and this update — `singularity-roadmap.md` (2026-07-26) confirms the current save state as hacking 15, $400, 32GB home RAM, "fresh into BN4." This is the single biggest state change since the last close: **`ns.singularity.*` is no longer gated** (BN4 grants native access without needing Source-File 4 — see the updated `[[bitburner_singularity_locked]]` memory), so the faction/augment/company-work automation that was shelved throughout the BN1 grind is now buildable. This session (2026-07-26, a pure doc-sync close — no code written in this conversation) closes out three unclosed prior-session commits: `d9e8a89` (w0r1d_d43m0n discovery popup, still BN1-era), `be612ad` (singularity roadmap + first two BN4 automation loops), and `7494d0e` (RAM-priority gating fix). Working tree is clean and already pushed to `origin/main`.

**BN4 singularity automation — new this cycle, ranked via `singularity-roadmap.md` (built via `/interview` 2026-07-26):**
- `company-work-loop.ts` (Tier 1, no prerequisites) — applies to Software-track jobs across every megacorp in `COMPANY_CANDIDATES` (ranked by salary/exp multiplier per `jobs.md`), keeps whichever company accepts, then works that job on a 30s poll loop via `ns.singularity.applyToCompany`/`workForCompany`. Chain-launched first among the ranked primitives since it needs no prerequisites and funds everything downstream.
- `home-ram-loop.ts` (Tier 2, needs Tier 1's money) — calls `ns.singularity.upgradeHomeRam()` every 30s. Wired ahead of `battlestation.ts` in the boot sequence since it's the actual fix for the home-RAM ceiling that was blocking `company-work-loop.ts` (and the `backdoor-loop.ts`/`darknet-manager.ts` tail) from launching at all on this fresh, RAM-starved BN4 save. Exempted from the 64GB gate below — it's a must-run priority, not a lower-priority manager.
- `jobs.md` (new reference doc, compiled via `/research`, 5 parallel agents against `bitburner-official/bitburner-src`'s `dev` branch source — not community guides) ranks hacking-XP/money sources: best company job is Software → Chief Technology Officer (`software7`); best faction work type is Hacking Contracts; best crime is Heist; best free hacking-XP method is Study Computer Science at any university. Grounds `company-work-loop.ts`'s candidate ordering and future faction-work-loop scoping.
- `singularity-roadmap.md` (new) is the dependency-graph doc driving build order: Tier 1 (company work, no prereqs) → Tier 2 (home RAM/Core upgrades needs money; faction work needs hacking level past invite threshold + `backdoor-loop.ts`'s existing CSEC grab). Backdoor automation and program acquisition (via the existing darknet stack) are marked "already done," not re-ranked.

**RAM-priority gating — fixed this cycle (`7494d0e`), root-causing a real in-game starvation bug:** on a small, fresh BN4 home server, actual hacking (weaken/grow/hack dispatch) kept losing the RAM race to resident managers (`hacknet-manager.ts`, `battlestation.ts`, `company-work-loop.ts`, etc.) that launched unconditionally at boot — dispatch got whatever scraps were left, often nothing.
- `hasEnoughHomeRam(ns, thresholdGb)` (new, `src/lib/launch.ts`) is a 0-RAM-cost helper (`ns.getServerMaxRam("home") >= thresholdGb`) gating every non-essential manager behind a `LOWER_PRIORITY_HOME_RAM_THRESHOLD_GB = 64` floor.
- Only `scan-root.ts`, `rescan-loop.ts`, `controller.ts` itself, and the weaken/grow/hack dispatch loop are allowed to run unconditionally now. `home-ram-loop.ts` is also always-on (it's the actual fix for the ceiling, not a competitor for it) but is launched from inside `controller.ts`'s dispatch loop, after each tick's own dispatch has already claimed its RAM — not from the chain-launch tail.
- Gated behind `hasEnoughHomeRam(ns, LOWER_PRIORITY_HOME_RAM_THRESHOLD_GB = 64)`, re-tried every loop tick via plain non-blocking `ns.run()` (not the blocking, sleep-between-attempts `runWithRetry()`, which would stall dispatch): `hacknet-manager.ts`/`battlestation.ts`/`server-purchase-manager.ts` from inside `controller.ts`'s dispatch loop, and `company-work-loop.ts`/`backdoor-loop.ts` from inside `rescan-loop.ts`'s cycle loop. Since `backdoor-loop.ts` still unconditionally launches `darknet-manager.ts` once *it* starts, the whole darknet tail now transitively waits on the same 64GB threshold too — a real behavior change from pre-`7494d0e`, where darknet automation started immediately regardless of home RAM.
- `rescan-loop.ts` now launches directly from `scan-root.ts` instead of from `hacknet-manager.ts` — it was getting stuck behind an already-deprioritized script under the new gating, which would have broken retargeting entirely on a RAM-starved save.

**w0r1d_d43m0n discovery popup (`d9e8a89`, BN1-era, now historical):** `scan-root.ts` pops an in-game alert the first time `w0r1d_d43m0n` appears in a scan, since it only becomes reachable after The Red Pill is installed. `bitnodes.md` was corrected with the source-verified detail that WorldDaemon has no `networkLayer` and is only wired onto the network from `Prestige.ts`'s `prestigeAugmentation()`. This shipped before the BN1→BN4 transition; kept for the record since `bitnodes.md`'s win-condition research still applies to every future BitNode completion, including BN4's.

**Core automation — live and working, a self-assembling chain (no `activate.ts`):**
- Entrypoint is typing `run scan-root.js` directly at the in-game terminal — not a wrapper script. A script whose only job is one `ns.run()` call costs ~2.6GB (1.6GB base + 1.0GB for `run`) for zero benefit, since the terminal itself is free.
- Chain order: `scan-root.ts` (recon + root, writes `/data/servers.json`) → `controller.ts` (weaken/grow/hack dispatch loop) → `hacknet-manager.ts` (auto-purchases hacknet upgrades) → `rescan-loop.ts` (re-runs `scan-root.js` every 30s so `controller` always retargets the top-payout server) → `backdoor-loop.ts` (walks every rooted host, installs a backdoor via `ns.singularity.connect`/`installBackdoor` once possible) → **`darknet-manager.ts`** (new tail — see below). Each script launches the next itself at the top of its own `main()`, then either exits (`scan-root.ts`) or settles into its own persistent loop — no single orchestrator stays resident through the whole sequence. The current tail carries a `// CHAIN-TAIL` marker (now in `darknet-manager.ts`) so `new-background-loop` knows where to wire in the next scaffolded script.
- `controller.ts` also directly launches (not via the chain-tail marker) `battlestation.ts` (live tail-window HUD: money/income, root status, network RAM, active hack/grow/weaken jobs) before its own thread-count sizing, so its RAM is reserved rather than competing with an already-sized batch. It prints the current hack target to the terminal on attack/target-switch.
- **`server-tree.ts` is deliberately NOT chain-launched** (reverted this session, `957e591`) — run it by hand (`run scripts/server-tree.js`) whenever wanted. `controller.ts` still reserves its RAM cost (queried live via `ns.getScriptRam`, ~1.95GB) on top of `DARKNET_RAM_RESERVE_GB` before sizing weaken/grow/hack batches, so launching it manually is never blocked by a batch that already claimed that headroom.
- **`hacknet-manager.ts` buys any positive-marginal-gain purchase with no payback-period ceiling** (`RESERVE_FRACTION` 10% is the only floor). A 30-minute payback cap was tried and then reverted (`2039b97`, prior session): it didn't scale with game progression — as node costs compounded, the cap made the manager stop buying almost everything, stalling hacknet income instead of protecting it. The existing payback-ratio greedy ordering already prioritizes the best available purchase each tick, which was judged sufficient on its own.
- **`hacknet-manager.ts`'s `moneyGainRate` is back to the hand-rolled approximation, permanently** (reverted 2026-07-23, after briefly calling the real `ns.formulas.hacknetNodes.moneyGainRate()` per commit `14e0c23`) — an augment install wiped `Formulas.exe` mid-session ([[bitburner_augment_reset]] in memory: this happens on *every* aug install, not a one-off), crashing the script with a runtime error. Since every home `.exe` gets wiped on every install, any hard dependency on one will break the same way again; the approximation is only used for relative purchase ordering, so its imprecision doesn't matter. Not revisiting this even once SF-granting augments make Formulas.exe convenient to re-buy.
- Worker scripts `hack.ts`/`grow.ts`/`weaken.ts` are minimal, byte-identical single-`ns`-call scripts, dispatched by `controller.ts` via `ns.exec` across the whole host pool (`home` + every purchased server, via `ns.scp` on first sight of a new purchased host).
- **`controller.ts`'s dispatch model is now two-phase: continuous prep, then true HWGW batching** (2026-07-23, superseding the single-session-earlier "proportional simultaneous WGH" model). A target that isn't primed (security within `SECURITY_TOLERANCE` of min, money above `MONEY_THRESHOLD`) still gets plain continuous weaken/grow (`computePrepPlan`), same as before. Once primed, dispatch switches to self-contained hack→weaken→grow→weaken batches (`computeBatchPlan`/`dispatchBatch`), timed via `additionalMsec` so the four operations land in order, queued every `MIN_BATCH_PERIOD_MS` so many batches run concurrently per target instead of waiting a full round out — this is what actually lets a target's RAM demand scale past a few hundred GB. Multi-target working-set admission (`buildWorkingSet`) carried over unchanged in shape, just re-pointed at the new batch-based `estimateTargetDemandGb`. Not yet verified in-game — see Current Goals.
- **`server-purchase-manager.ts`** (new, this session) is a standalone chain-launched loop — launched directly by `controller.ts` (alongside `hacknet-manager.ts`/`battlestation.ts`, not via the `// CHAIN-TAIL` marker, so purchasing starts from `controller.ts`'s very first cycle) — that buys/upgrades purchased ("cloud") servers via the `ns.cloud.*` API: 10% cash reserve floor (same `RESERVE_FRACTION` pattern as `hacknet-manager.ts`), new servers start at 8GB, and each tick greedily takes whichever candidate (new server vs. doubling an existing one's RAM) is cheapest per GB gained, up to `ns.cloud.getServerLimit()`/`ns.cloud.getRamLimit()` (queried live, never hardcoded).
- `src/lib/network.ts` holds recon (scan/parent-map) helpers only; root-access logic (`tryRoot`) lives in `src/lib/root.ts`. `src/lib/launch.ts` holds `runWithRetry()`, the shared retry-on-RAM-block launcher used throughout the chain.
- Standalone (non-chain) scripts for manual use: `server-tree.ts` (see above) and `connect-to.ts` (walks `ns.singularity.connect()` hop-by-hop to reach any discovered host by name).
- Dev loop: `npm run watch` (`tsc -w`) + `npm run sync` (`bitburner-filesync`) run as detached background processes via the `dev-watch` skill.

**Darknet automation (new, `65790c7`, prior unclosed session) — a self-learning `ns.dnet` explorer/cracker, chained as the new tail after `backdoor-loop.ts`:**
- `darknet-manager.ts` stays free of `ns.dnet.*` calls itself (except the free `getDarknetInstability` throttle check) and dispatches short-lived worker scripts instead, keeping its own resident RAM cost low: `darknet-agent-recon.ts` (probes children, scrapes hints/logs), `darknet-agent-value.ts` (opens caches, reallocates memory), `darknet-crack.ts` (tries candidates: a "ZeroLogon" empty-password shortcut, then hint/data-mined candidates, then brute force). All three walk authenticated hops (`connectToSession` + `scp` + `exec`) to reach servers beyond the one directly connected to `home`, reporting results back through a shared Netscript port (`DARKNET_REPORT_PORT = 20`, since `ns.read`/`ns.write` are per-host). State persists to `/data/darknet-kb.json`, including cross-server learned-pattern rankings.
- **`ns.dnet` is not Singularity-gated** — it works today even though the save has zero Source-Files (see the Singularity note below, which still applies to everything *else*).
- **This session (`56256ba`) fixed a live crash**: `ns.dnet.connectToSession` throws `Invalid host` (not a graceful `{success:false}`) when a hop has moved/gone offline mid-path — confirmed in-game via a popup runtime error (`darknet-agent-value.js`, host `chongq1ng`). All three worker scripts now wrap the hop-connect block in try/catch, and report the failure back to `darknet-manager.ts` via a new `hopFailed` port message. The manager marks that hop `status: "unresolvable"` and `buildPathTo` now excludes unresolvable servers, pruning every deeper descendant that routed through it — and `applyDiscovery` clears that status automatically if the same hostname is later legitimately rediscovered as a live child of its parent (proof it's actually back).
- `DARKNET_RAM_RESERVE_GB = 8` in `controller.ts` reserves room for the heaviest single darknet dispatch (`darknet-agent-value.js`, 6.80GB) so `darknet-manager.ts`'s own `ns.exec` calls aren't starved by an already-sized weaken/grow/hack batch.
- **`darknet-manager.ts` now tolerates a missing `DarkscapeNavigator.exe`** (2026-07-24, `a88282c`): an augment install wiped it (same standing pattern as every other home `.exe` — [[bitburner_augment_reset]]), crashing `ns.dnet.getDarknetInstability()` every 15s cycle. It can't be auto-repurchased (`purchaseProgram` is Singularity-gated, unavailable without SF4), so the script now checks `ns.fileExists("DarkscapeNavigator.exe", "home")` first, prints a one-time warning, and disables the instability throttle (crack dispatch keeps running) until the program is repurchased manually.

**Real, verified RAM cost per chain script — STALE, pre-BitNode-reset:** the ~16.25GB four-script steady-state figure (`scan-root.js`/`controller.js`/`hacknet-manager.js`/`rescan-loop.js`) and every `ram-audit` static estimate below it were measured against the old BN1 home server. The BN1→BN4 reset gave a fresh 32GB home server (per `singularity-roadmap.md`), so **none of these numbers can be trusted until re-verified via in-game `mem <script>` on the new save** — treat the whole table as historical reference for *shape* (which scripts are RAM-heavy) only, not current headroom math. `backdoor-loop.js` calls `ns.singularity.connect`/`installBackdoor`, both Singularity functions — these are **no longer SF4-gated** now that the save is natively inside BitNode 4 (see Current Project State), so `backdoor-loop.js`'s previous "effectively a no-op, idles/retries" status is also stale and needs re-confirming in-game.
- `scan-root.js` 3.80GB · `controller.js` 4.85GB (pre-HWGW-batching figure, already known stale even before the reset) · `hacknet-manager.js` 8.70GB · `rescan-loop.js` 2.70GB (all pre-reset).
- `ram-audit` static estimates (pre-reset, not `mem`-verified even then): `controller.ts` 11.50GB, `battlestation.ts` 4.15GB, `server-purchase-manager.ts` 6.15GB, `server-tree.ts` 1.95GB, `darknet-manager.ts` 3.00GB, `darknet-agent-recon.ts` 3.55GB, `darknet-agent-value.ts` 3.75GB, `darknet-crack.ts` 3.50GB.
- `company-work-loop.ts` and `home-ram-loop.ts` are brand new this cycle and have **no RAM figure at all yet**, static or `mem`-verified — both call `ns.singularity.*` methods not yet in `ram-audit`'s cost table (`applyToCompany`, `workForCompany`, `upgradeHomeRam`); run `ns-cost-lookup` against them before trusting any estimate.

**Tooling — 10 project-local Claude Code skills:**
- `activate-check` — walks the chain-launch graph from `scan-root.ts` (BFS via `// scripts/....js` references) instead of a single launch-list file; confirms every reachable script exists and flags unwired background-loops.
- `check-unlock` — checks whether a newly-unlocked Bitburner program needs `ns.*` wiring, logs the finding in progression memory. Its doc pointer to the port-opener list was stale (still said `src/lib/network.ts`, moved to `src/lib/root.ts` in an earlier refactor) — fixed 2026-07-24 via `skill-audit`.
- `dev-watch` — start/stop/status for the watch+sync background processes.
- `new-background-loop` — scaffolds a new persistent loop script and wires it onto the current chain tail (via the `// CHAIN-TAIL` marker), not into a launch-list file. Its `scaffold-loop.sh` had a real, twice-recurring bug (GNU `sed`'s `a\` command misparsing a multi-line append as new addressless commands, once silently replacing an entire target file with garbage) that was documented as a Gotcha after the first hit but never actually fixed in the script — so it recurred identically on the next scaffold call. **Actually fixed this session** (`77b8f53`): the const-block insertion now writes to a temp file and uses sed's `r <tempfile>` read-in command instead of raw `a\` text, verified against a scratch clone of the repo. Its boot-chain example doc was also two hops stale (fixed 2026-07-24, `e752399`) — reworded to point at the live `// CHAIN-TAIL` marker instead of a hardcoded chain description.
- `new-worker-script` — scaffolds a `hack.ts`-style single-`ns`-call worker script. Its scaffold template had drifted to the old 1-arg worker shape after `hack.ts`/`grow.ts`/`weaken.ts` gained a 2-arg `(target, additionalMsec)` shape for HWGW batching — fixed 2026-07-24 (`47f3671`), verified regenerated output is byte-identical to `hack.ts`.
- `ram-audit` — static RAM cost estimator for every script in `src/scripts`; flags (⚠) two confirmed Bitburner-engine RAM-analyzer bugs that this estimator can't itself account for (see Known Issues). Its cost table (`ram-costs.json`) had a dead entry (`openDevMenu`, no longer in `NetscriptDefinitions.d.ts`) — dropped 2026-07-24 via `skill-audit`.
- `ns-cost-lookup` — resolves the exact documented RAM cost of one or more `ns.*` functions from `NetscriptDefinitions.d.ts`, properly scoped to the real interface/sub-interface signature rather than a blind grep. Fixed 2026-07-24 (`dcfaa62`, via `skill-audit`): it silently dropped the `* 16/4/1` scaling suffix on tiered-cost entries (all Singularity methods — e.g. `singularity.connect` resolved to 2GB instead of the correct 32GB), and `findInterfaceBody()` hard-required an `export` keyword, so every `ui.*` lookup errored since `UserInterface` is declared without one.
- `reorder-chain-launch` — relocates an already-wired chain-launch call to a different point in the boot chain via exact-match edits.
- `position-tail-window` (new, 2026-07-24, `7841705`) — knowledge/judgment skill for positioning/resizing a script's tail window (`openTail`/`resizeTail`/`moveTail`) against real screen dimensions. Built after noticing 13 past sessions had independently rediscovered the same `ns.ui.windowSize()` quirks (reports the full browser window, not the usable game area; no API exists for the Overview panel's width, so this repo reuses a measured estimate `OVERVIEW_WIDTH = 220` calibrated off a 1920px screenshot; a fresh script's tail can look mispositioned on its first render and self-correct next frame). No `scripts/`/`assets/` — this is judgment work (interpreting pixel-delta feedback), not templated.

**Also new this session (repo-managed, not bitburner-specific — live in `~/.claude`, not this repo):** `commit-and-push` (chains `git-commit` → `git-push`), and `git-commit` now asks before silently leaving unrelated uncommitted work out of a commit. See the NixOS dotfiles repo for these, not this one.

**`bitnodes.md` (2026-07-24, `8f2004e`/`fed54e5`)** — a reference doc on every BitNode's win condition, compiled via `/research` (6 parallel agents against the game's own source, official docs, and community guides) after the user asked what BN1's win condition was at $34T net worth. Key finding, still true for every future BitNode including BN4: **money is only a gate, not the win condition** — get invited to Daedalus ($100b net worth + 30 augmentations + (hacking ≥2500 OR all combat stats ≥1500)), earn enough faction rep for The Red Pill augmentation, install it, then manually hack `w0r1d_d43m0n` at hacking level 3000+. The game's source (`BitNode.tsx`) only defines BitNodes 1–15 currently, not 24+. **BN1 has since been completed** (per the fresh-BN4 save state confirmed 2026-07-26 — see Current Project State); this doc's win-condition mechanics are the roadmap for BN4's eventual completion too.

**Tail-window sizing convention (established this session across `battlestation.ts`/`server-tree.ts`):** compute height dynamically from `ns.ui.windowSize()` (0 GB RAM) minus a `TAIL_HEIGHT_MARGIN = 100` px browser-chrome allowance, rather than a fixed pixel constant — so the HUD scales with the actual screen instead of clipping or leaving gaps on other resolutions. Now baked into the `position-tail-window` skill above.

## Current Goals

**Short-term (next 1-3 sessions):**
- **Verify the RAM-priority gating fix (`7494d0e`) in-game**: confirm weaken/grow/hack dispatch actually gets RAM first on the fresh, small BN4 home server, that `home-ram-loop.ts` climbs `home`'s max RAM over time, and that `hacknet-manager.ts`/`battlestation.ts`/`server-purchase-manager.ts` do eventually launch once `hasEnoughHomeRam(ns, 64)` passes rather than starving forever. This is a fix for a real observed in-game symptom (dispatch getting no RAM), not yet confirmed resolved.
- **Verify `company-work-loop.ts` in-game**: confirm it successfully applies to and gets hired at one of the `COMPANY_CANDIDATES`, that `workForCompany` actually accrues salary/rep on a 30s cadence, and that it survives an augment install (no home-program dependency, so it should — unlike the `Formulas.exe`/`DarkscapeNavigator.exe` pattern — but not yet observed live).
- **`company-work-loop.ts` and `home-ram-loop.ts` need RAM costs established** (static via `ram-audit`/`ns-cost-lookup`, then `mem`-verified in-game) — currently no figure at all, static or real, for either script.
- ~~Wire `company-work-loop.ts` into the same gate~~ — confirmed already done: it's gated via `rescan-loop.ts` (not `controller.ts`), alongside `backdoor-loop.ts`, both behind the same `hasEnoughHomeRam(ns, 64)` check. This also means `darknet-manager.ts` (launched unconditionally from inside `backdoor-loop.ts`) now transitively waits on the 64GB threshold too, since `backdoor-loop.ts` itself won't start before then — a real behavior change worth watching for: darknet automation no longer starts at low home RAM the way it did pre-`7494d0e`.
- **Re-verify every chain script's RAM cost via in-game `mem`** now that the BN1→BN4 reset invalidated all prior figures (fresh 32GB home server, not the old BN1 one) — see Current Project State.
- Continue down `singularity-roadmap.md`'s dependency graph once Tier 1/2 are verified live: Tier 2's faction-work loop needs hacking level past the relevant faction's invite threshold plus `backdoor-loop.ts`'s existing CSEC grab — check current hacking level before scoping it.
- Watch `darknet-manager.ts`'s logs for `marked unresolvable (hop failed)` lines — confirms the 2026-07-23 hop-failure fix is actually pruning dead paths in the running game, not just passing a build. Still unconfirmed as of the last close and this one didn't touch it.

**Long-term:**
- As new programs unlock, use `check-unlock` to keep `NetscriptDefinitions.d.ts` wiring current and progression memory accurate.
- If a new Bitburner RAM-analyzer phantom-charge trigger is ever found, add it to `ram-audit`'s `extractRiskFlags()` and the "Known false negatives" section of its `SKILL.md` (only two are documented so far — the list is known to be incomplete).
- **Singularity automation is now unblocked and actively being built** (BN4 grants native access without SF4 — see Current Project State and the updated `[[bitburner_singularity_locked]]` memory). Continue working down `singularity-roadmap.md`'s tiers: company work (done) → home RAM upgrades (done) → faction work loop → augmentation purchasing, per the brief scoped-then-shelved in the 2026-07-20 interview (cheapest-first buying with a percentage cash floor, manual install, joined-factions-only scope, best-available work type per faction, NeuroFlux Governor bought last) — that old brief is now buildable, revisit it rather than re-scoping from scratch.
- Toward BitNode 4 completion (this save's current BitNode): per `bitnodes.md`, the win path is Daedalus invite ($100b net worth + 30 augmentations + hacking ≥2500 or all combat stats ≥1500) → Red Pill rep grind/install → hack `w0r1d_d43m0n` at hacking ≥3000. All still manual, in-game-UI actions — none of the three are scriptable via `ns.*` even with Singularity unlocked (no `ns` function grants an augmentation directly or hacks the win-condition server for you).

## Recent Decisions

- **This session (2026-07-26) was a pure documentation close-out — no code was written in this conversation.** The three commits it closes out (`d9e8a89`, `be612ad`, `7494d0e`) were made in a prior, unclosed session; their rationale below is transcribed from commit messages and source diffs, not re-derived from a live conversation transcript (the transcript for this session contains only the `/session-closer` invocation itself).
- **BitNode 1 was completed and the save reset into BitNode 4** (observed between the 2026-07-24 and 2026-07-26 closes, via `singularity-roadmap.md`'s "fresh into BN4" save-state note) — the single biggest state change this cycle. Flips the standing "don't scope `ns.singularity.*` scripts" guidance from every prior close: Singularity is BN4-native, no Source-File 4 needed while inside BN4. Updated `[[bitburner_singularity_locked]]` memory to record this.
- **Gated lower-priority managers behind a 64GB home-RAM threshold** (`7494d0e`) rather than continuing to launch everything unconditionally at boot — a fresh, small BN4 home server meant actual hack/grow/weaken dispatch was losing the RAM race to resident managers and getting no RAM at all. Chose a re-tested-every-tick, non-blocking `ns.run()` gate over one-shot deferred launch so managers start automatically the moment RAM allows, without a dedicated "am I unblocked yet" poller script.
- **Ranked BN4 singularity-automation build order via `/interview` + `/research` before writing code** (`singularity-roadmap.md`, `jobs.md`, 2026-07-26) — reused the standing pattern from the BN1-era purchased-server work (interview to scope the ask, research for the ground truth) rather than guessing job/faction priority from memory. `jobs.md` pulled numbers directly from `bitburner-src`'s own source files since the official docs' companies page is an unfilled stub.
- **Built `company-work-loop.ts` as the first BN4 automation loop, ahead of faction/augment work**, since it has no prerequisites and its money output funds every other Tier-2+ item in the roadmap (home RAM upgrades, faction-work gear/travel, augmentation purchases).
- **Ran a full `skill-audit` pass and fixed every real bug it found in the same pass, rather than just reporting them** (2026-07-24): `ns-cost-lookup`'s tiered-cost scaling drop, `new-worker-script`'s stale 1-arg scaffold template, and three stale doc pointers across `check-unlock`/`ram-audit`/`new-background-loop`. Consistent with the standing "a documented gotcha with an unapplied fix behaves like an undocumented one" rule from the 2026-07-23 close.
- **Built `position-tail-window` as a project-local skill instead of leaving tail-window placement as one-off trial-and-error** (2026-07-24) — 13 past sessions had independently rediscovered the same `ns.ui.windowSize()` quirks. Scoped as a knowledge/judgment skill (no `scripts/`/`assets/`) since positioning requires interpreting the user's pixel-delta feedback, not templated generation. Smoke-tested with a real (then-reverted) nudge to `server-tree.ts` before committing.
- **Compiled `bitnodes.md` via `/research` rather than answering BitNode win-conditions from memory** (2026-07-24) — the official docs' "BitNode Details" section is an unwritten TODO; the authoritative answer came from the game's own `BitNode.tsx` source, cross-checked against a comprehensive community strategy guide. Corrected a first-pass omission (30-augmentations requirement for Daedalus) once the user pointed it out.
- **Made `darknet-manager.ts` tolerate a missing `DarkscapeNavigator.exe` rather than crash** (2026-07-24, `a88282c`) — can't auto-repurchase it (Singularity-gated, no SF4), so the fix is graceful degradation (disable the instability throttle, warn once) instead of a hard dependency, following the same standing pattern already applied to `Formulas.exe` in the 2026-07-23 close.
- **Implemented the "tier 3" true HWGW batching that the prior session's dispatch-model research deliberately deferred** (2026-07-23), after the user noticed almost all purchased-server RAM sat unused in-game (~0.5% of ~1.6PB) despite `server-purchase-manager.ts` correctly refusing to buy more. Root cause: the tier-2 proportional-simultaneous-WGH model's per-target RAM demand is capped by the target's own live security/money state, not by available RAM — once every candidate target's one-round demand is funded, extra RAM has nowhere to go. Two design calls made with the user via `AskUserQuestion` before writing code: (1) skip `Formulas.exe` and keep recomputing each batch's `additionalMsec` delays from live `getHackTime`/`getGrowTime`/`getWeakenTime` right before that batch launches, both to avoid a new dependency on a program that gets wiped every augment install ([[bitburner_augment_reset]]) and to sidestep the desync-under-precompute fragility the tier-2 research flagged; (2) wire batching into the existing multi-target working set in the same pass rather than shipping single-target-only first. `computeTotalDemandGb`'s ceiling is now timing-derived (`batchDurationMs / MIN_BATCH_PERIOD_MS` concurrent batches per target) rather than RAM-derived, so `server-purchase-manager.ts`'s stop-buying signal stays meaningful at the new, much higher demand level. Not yet verified in-game (build + `ram-audit` clean; introduced and then fixed three new `??` phantom-RAM-charge triggers per the confirmed pattern in Known Issues before final build).
- **Scoped (via `/interview`) and built purchased-server automation + a dispatch-model rework, chosen via `/research` rather than assumed** (prior session): the ask was "automate cloud servers and spread weaken/grow/hack across them for the highest money/sec." Five sources (Bitburner hacking-algorithms docs, a Steam HWGW-manager guide, a Steam batching discussion thread, and two community GitHub script repos) were reviewed in parallel and converged on a three-tier picture: (1) naive single-script-type-per-cycle (what `controller.ts` did before this session) is the weakest, leaving most RAM idle between launches; (2) **proportional/ratio-tuned simultaneous dispatch** (weaken+grow+hack fired together each cycle, thread counts computed off live server state) is the practical sweet spot for early-mid game — RAM-efficient, no precise timing required; (3) **true continuous HWGW batching** (hack→weaken→grow→weaken landing in a precisely 20-200ms-spaced sequence) is the theoretical income ceiling but needs >1TB RAM per target to pay off and was reported fragile in practice by real players — rising hacking level shortens op durations over time and desyncs a precalculated schedule. Decision: built tier 2 for v1 (`controller.ts`'s "Dispatch model" comment block has the same writeup in-code), explicitly deferred tier 3 and multi-target dispatch, and documented both as the next expansion step so a future session doesn't have to redo this research. A second-AI review of the brief (before implementation) caught three real gaps folded into the build: `ns.scp` was needed but unmentioned, weaken sizing needed the *security-side* analysis functions (`growthAnalyzeSecurity`/`hackAnalyzeSecurity`), not just the money-side ones, and the new `server-purchase-manager.ts` needed to launch directly from `controller.ts` (not the `// CHAIN-TAIL` marker) so purchasing starts on `controller.ts`'s first cycle rather than after the whole rest of the boot chain.
- **The actual purchased-server API in this game version is `ns.cloud.*`, not the classic flat `ns.purchaseServer`/`ns.getPurchasedServers`** most community guides (written for older Bitburner versions) reference — confirmed by reading `NetscriptDefinitions.d.ts` directly rather than trusting research-source code samples. `server-purchase-manager.ts` and `controller.ts`'s host-pool logic both use `ns.cloud.getServerNames()`/`getServerLimit()`/`getRamLimit()`/`getServerCost()`/`purchaseServer()`/`getServerUpgradeCost()`/`upgradeServer()`.
- **Reverted the 30-minute Hacknet payback cap** (`2039b97`, prior unclosed session) — it didn't scale with progression: as node costs compounded exponentially, more and more purchases exceeded the flat cutoff and the manager stopped buying anything, stalling income instead of protecting it. Back to unbounded `gain > 0` acceptance; the existing payback-ratio greedy ordering already prioritizes the best available purchase each tick. (Supersedes the "capped at 30 minutes" decision from the 2026-07-21 close — that cap is gone.)
- **Reverted `hacknet-manager.ts` back off `ns.formulas.hacknetNodes.moneyGainRate()` to the hand-rolled approximation, permanently** (2026-07-23, superseding `14e0c23` from the same day) — an augment install wiped `Formulas.exe` mid-session and crashed the script with a runtime error. Since every home `.exe` is wiped on *every* augment install, not just this once, this repo now treats any hard dependency on one as a standing bug rather than something to fix opportunistically each time it recurs; the approximation only affects relative purchase ordering, so the lost precision doesn't matter.
- **Added self-learning darknet automation** (`65790c7`, prior unclosed session): `darknet-manager.ts` dispatches short-lived `ns.dnet` worker scripts (recon/value/crack) rather than calling `ns.dnet.*` itself, keeping its own resident RAM low; state persists to `/data/darknet-kb.json` with cross-server learned-pattern rankings. Chained onto `backdoor-loop.ts` as the new tail.
- **Fixed a live darknet crash and closed the loop on stale hops** (this session, `56256ba`): `ns.dnet.connectToSession` throws (not a graceful failure) when a hop has moved/vanished, which had crashed a running worker script in-game. All three worker scripts now try/catch the hop-connect block and report the failure back via a new `hopFailed` port message; `darknet-manager.ts` marks that hop `unresolvable` (excluded from `buildPathTo`, pruning every deeper descendant too) and automatically recovers it if legitimately rediscovered later.
- **Took `server-tree.ts` out of the chain-launch** (this session, `957e591`) — reverted the prior session's auto-launch (`83eb468`) in favor of manual control; its RAM cost is still reserved (via live `ns.getScriptRam`, not a hardcoded guess) so launching it by hand is never blocked.
- **Actually fixed `scaffold-loop.sh`'s CHAIN-TAIL corruption bug** (this session, `77b8f53`) rather than re-documenting it: a prior gotcha had diagnosed the multi-line `sed a\` insertion bug and prescribed a fix, but the fix was never applied to the script, so it recurred identically on the very next scaffold call (forcing manual chain wiring both times — see how `battlestation.ts`/`server-tree.ts` ended up hard-wired into `controller.ts` instead of the automatic tail). Now uses a temp-file + sed `r` read-in, verified against a scratch clone of the repo.
- **Added `battlestation.ts` as a second HUD launched alongside `hacknet-manager.ts`, and `reorder-chain-launch` as a dedicated skill for repositioning chain-launch wiring** — carried over from a prior session not closed out before this one; recorded here from the commit history so `project-state.md` reflects current reality, but the rationale above is only as recorded in those commits, not re-derived from this session's conversation.
- **Replaced `activate.ts` with a self-assembling chain-launch bootstrap.** `activate.ts` stayed resident through the entire multi-script launch sequence, so its own RAM cost stacked on top of every already-launched persistent script — on a small fresh-reset `home` this could exceed available RAM outright, and retries didn't help since nothing freed RAM between attempts. Now each script launches the next itself, then exits or moves into its own loop; `activate.ts` was deleted entirely since a dedicated launcher script is now pure overhead (the terminal is free; a script isn't).
- **Root-caused a second, more fundamental problem after the chain redesign: Bitburner's own static RAM analyzer has confirmed bugs.** It emits a large phantom charge (`10GB | codingcontract.attempt`) completely unrelated to a script's real `ns.*` usage, in two confirmed cases: (1) an `ns.*` call made indirectly through a closure stored in an object/array (`{ run: (ns, host) => ns.brutessh(host) }`, invoked as `opener.run(...)`) — fixed in `lib/network.ts`'s `tryRoot` and `hacknet-manager.ts`'s `Purchase.apply`; (2) the `??` (nullish-coalescing) operator, which alone triggered the same charge in an otherwise-clean file — fixed in `controller.ts`. This was found via live `mem <script>` bisection, not by reading code — a script can look cheap by inspection and still get charged 10GB+ for something it never calls. Comments mentioning `ns.*` names do **not** trigger this — confirmed the analyzer ignores comments.
- **`ram-audit` now warns (⚠) about both confirmed phantom-charge triggers** rather than silently under-reporting. Its core premise ("static cost = accurate sum") had to be caveated: true for well-behaved code, not guaranteed against the game's own engine bugs.
- **Built `ns-cost-lookup`** instead of continuing to look up RAM costs by hand — the manual `grep`/`sed` line-offset approach had misfired repeatedly this session (wrong overload, wrong window, a function's real cost missed entirely) and across every prior session transcript for this project (confirmed via grep). The new tool properly resolves the `NS`-interface (or sub-interface) signature and its doc comment; verified against every ground-truth cost confirmed via `mem` this session, and it caught a cost I'd misread by hand (`print()` does have `RAM cost: 0 GB` documented — I'd read too narrow a window and concluded it didn't).
- **Split `tryRoot` out of `network.ts` into its own `lib/root.ts` module**, and added `backdoor-loop.ts` to the end of the chain (after `rescan-loop.ts`) to auto-backdoor every rooted host once SF4 makes that possible.
- **Scoped (via `/interview`) a request to automate faction reputation grinding and augmentation purchasing, then shelved it before writing any code.** Checked `NetscriptDefinitions.d.ts` directly rather than assuming: the entire `Singularity` interface — `workForFaction`, `purchaseAugmentation`, `getAugmentationsFromFaction`, `getFactionRep`, `purchaseTor`, `purchaseProgram`, etc. — carries a hard requirement of Source-File 4 to use outside BitNode 4 (not just an expensive RAM multiplier; it throws at runtime). This save has zero Source-Files, so the feature is currently unbuildable. Decision: pivoted to giving manual in-game-UI strategy advice instead of writing dead code, and recorded the finding in memory (`bitburner_singularity_locked`) so it isn't re-discovered from scratch next time this comes up.

## Known Issues / Tech Debt

- **`home` RAM headroom is currently unknown on the new BN4 save** — the old ~16.25GB BN1 figure no longer applies (fresh 32GB home server post-reset). Nothing has been `mem`-verified since the reset. Re-verify the whole chain via in-game `mem` before assuming a specific free-RAM number, and before trusting whether the new 64GB lower-priority gate (`7494d0e`) is set at a sane threshold for this save's actual growth curve.
- **The RAM-analyzer phantom-charge trigger list (2 items) is almost certainly incomplete.** It reflects exactly what's been hit and confirmed in this repo, not a general survey of the game's analyzer. Treat an implausibly-high `mem` result on a new script as a signal to search for a new triggering shape, not just accept it.
- **`company-work-loop.ts`/`home-ram-loop.ts` are unverified end-to-end** — build clean, but neither has been confirmed to actually work in-game yet (hire successfully, accrue rep/salary, upgrade home RAM). See Current Goals.
- **`darknet-manager.ts`'s hop-failure handling is untested against the live crash that prompted it** — the fix (2026-07-23 close) was verified via `npm run build` and code review only, not by reproducing the actual in-game "Invalid host" scenario. Watch for `marked unresolvable (hop failed)` log lines to confirm it fires and behaves as designed.
- **`darknet-manager.ts`'s `DarkscapeNavigator.exe`-missing tolerance (2026-07-24) is also only build-verified, not confirmed against a real in-game crash-then-recover cycle** — confirm the one-time warning prints and the instability throttle actually re-enables once the program is repurchased. Note this program (like all home `.exe`s) will have been wiped by the BN1→BN4 reset too, same as every augment install — [[bitburner_augment_reset]].
- `check-unlock`'s memory-file path is a hardcoded absolute path (machine/session-specific). Low priority; not fixed.
- `dev-watch` hardcodes the npm script names `"watch"`/`"sync"` in two places (script + prose); low priority, not worth a shared-config split for a pair this small.

## Next Steps

- Verify the `7494d0e` RAM-priority gating fix in-game: dispatch gets RAM first, `home-ram-loop.ts` climbs max RAM, gated managers eventually launch once past 64GB.
- Verify `company-work-loop.ts` gets hired and accrues rep/salary in-game; establish its RAM cost (`ns-cost-lookup` then `mem`).
- Watch whether the darknet tail (`darknet-manager.ts` and its workers) actually starts once `home` clears 64GB — it's now gated transitively via `backdoor-loop.ts`'s own gate in `rescan-loop.ts`, a behavior change from before `7494d0e` where it started immediately.
- Re-`mem`-verify every chain script now that the BN1→BN4 reset invalidated all prior RAM figures.
- Watch `darknet-manager` logs in-game to confirm the hop-failure fix actually prunes dead paths (`marked unresolvable (hop failed)`) rather than just compiling clean.
- Run `server-tree.js` manually (`run scripts/server-tree.js`) whenever a network-tree view is wanted — it's no longer auto-launched.
- Next program unlock → run `check-unlock`.
- Next persistent automation idea → run `new-background-loop` (wires onto the chain tail automatically).
- New `ns.*` RAM cost needed → run `ns-cost-lookup` instead of a manual grep.
- Next tail-window placement/resize request → run `position-tail-window` instead of re-deriving the `windowSize()` quirks from scratch.
- Continue `singularity-roadmap.md`'s Tier 2+ items once Tier 1 is verified live: faction work loop, then revisit the shelved augmentation-purchasing brief (cheapest-first, cash-floor, manual install, joined-factions-only, NeuroFlux last).
- Toward BitNode 4 completion (this save's current BitNode): per `bitnodes.md`, Daedalus invite → Red Pill rep grind/install → hack `w0r1d_d43m0n` at hacking ≥3000 — all manual, none scriptable even with Singularity unlocked.
