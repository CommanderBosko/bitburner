# Project State

_Last updated: 2026-08-19_

## Current Project State

**2026-08-19: `backdoor-loop.ts` now auto-completes the BitNode via `w0r1d_d43m0n`, and BN4.2 is confirmed complete (SF4.2 obtained) — BN4.3, the third and final clear toward SF4.3, is now underway.** Two independent items, same session. **(1)** `NetscriptDefinitions.d.ts`'s own doc on `destroyW0r1dD43m0n()` ("with the hacking route, using installBackdoor is a cheaper way to do this") directly contradicted this repo's existing `backdoor-loop.ts` comment, which claimed `w0r1d_d43m0n` is deliberately excluded and destroyed via `ns.hack()` instead. That comment was wrong — backdooring `w0r1d_d43m0n` itself (once rooted, with the Red Pill aug/hacking-level requirement already covered by the loop's existing `The-Cave` gate) is what destroys the BitNode. Added `w0r1d_d43m0n` to `TARGET_HOSTS` (`2c41d2f`) so the loop triggers this automatically the next time it's reachable and rooted, rather than requiring a manual backdoor; corrected the stale comment in the same commit. Build-checked clean, synced into the running game — **not yet exercised live** (no `w0r1d_d43m0n` reachable this session; will first fire whenever a BN4.x clear reaches it next). **(2)** User confirmed BN4.2 completed for real (SF4.2 now owned, outside-BN4 Singularity RAM multiplier dropped 16x→4x) and has started BN4.3 — the third and final clear needed for SF4.3 (native 1x cost everywhere). No specifics on this session's BN4.2 endgame were reported (territory-warfare threshold firing, NFG-donation branch firing, etc. — see Known Issues), so those items are carried forward as still-unconfirmed rather than assumed resolved; BN4.3 is a fresh BitNode instance (karma/gang/augments/cash all reset again) so they'll need fresh observation there regardless. `[[bitburner_bn4_singularity]]`, `[[bitburner_singularity_locked]]`, and `[[bitburner_bitnode_route]]` memory all updated to the SF4.2/BN4.3 state; `MEMORY.md` index lines refreshed to match.

**2026-08-17/18: BN4.2 loop-bug family grows two more variants (4th and 5th) in the augment-loop/faction-work-loop NFG saga, gang territory-warfare tightened to near-certain-win only, crime-loop's focus flag flip-flopped twice, and a real dev-watch operational bug found and worked around.** All via the `diagnose-loop-bug` skill except the user-directed threshold change. In order: **(1) `d34cdd8`, 08-17** — `crime-loop.ts`'s `commitCrime` reverted to `focus: true` (user-directed): the early-game Mug→Homicide karma grind toward founding a gang is the first priority right after a fresh BitNode start, so jumping the UI to the Work screen on every call is wanted here, unlike `faction-work-loop.ts`/`company-work-loop.ts` which stay `focus: false`. **(2) `406ca73`, 08-17** — added `loop-bug-investigator` as a project-local custom agent (read-only fan-out unit for `diagnose-loop-bug`'s steps 2-3, for when multiple loops misbehave at once), scaffolded alongside a global `transcript-scanner` agent in the NixOS repo from the same log-mining pass. **(3) `3e17746`, 08-18** — `crime-loop.ts`'s `focus` reverted *again*, back to `false`: no stated reason, just found the disruptive screen-jump not worth it after living with it a day; memory now flags a 3rd flip as worth asking "why" about instead of just toggling. **(4) `e0a8ce3`, 08-18** — gang-manager's territory-warfare engage/disengage hysteresis replaced 65%/55% with 99.5%/99% (user-directed: only clash at an effectively-guaranteed win, not the researched "60-65% is enough, wins snowball" default); 99.5% chosen over a literal `=== 1` check since `getChanceToWinClash()` only hits exactly 1 when a rival's power is literally zero, which won't happen while any rival holds territory. **Not yet observed live.** **(5) `6dca5e6`, 08-18** — NeuroFlux Governor made a valid `augment-loop.ts` donation target (4th variant of the NFG-gate family): NFG was hard-excluded from `donateTarget` even once nothing real was left to buy, so a $1.4T+ idle budget could only close NFG's rep gap via the slow ~30 rep/sec work grind. Live evidence was partial — a purchase did land, but via the pre-existing work-rep path, not the new donation path, since Sector-12's favor (76.883) was still below the ~150 donation threshold at test time. **(6) `24d82d4`, 08-18** — the *real* root cause of the recurring "grinding Sector-12 for NFG with no augs left" symptom turned out to be one layer up: `faction-work-loop.ts`'s `orderFactionsByAugmentGap` deliberately kept NFG's own rep gap in its work-target ranking, so a faction with zero real augmentations left never dropped out of contention and stayed the permanent (unproductive) work target — variant 4's donation fix was necessary but not sufficient. Fixed by excluding NFG from the ranking entirely, matching `augment-loop.ts`'s own exclusion; **confirmed live with decisive evidence**: Sector-12's rep went flat while the loop picked up BitRunners (3 real augmentations left) instead, money grew $694.865b → $2.079t in under an hour. **Also this session (no commit, `.dev-watch/` is gitignored):** `/dev-watch start` reported `sync: running` while the game's Remote API still wouldn't connect — root-caused to a stale-PID false positive (`kill -0` succeeded against a recycled PID that had become `tsc -w`'s own child, not the real dead `bitburner-filesync` process); fixed by a clean stop/start. See `[[bitburner_devwatch_pid_reuse]]`. **BN4.2 context**: the character already has a gang (Slum Snakes, 244k favor), Hack 1584, and hundreds of billions to trillions of dollars by this session — well past the karma-grind/gang-founding stage flagged as "immediate next" in the prior close.

**2026-08-16 (later, skill-infrastructure session): full skill-audit sweep — 5 correctness bugs fixed, 2 cross-cutting duplications de-duplicated, diagnose-loop-bug's naming-guess grep extracted to a script.** No gameplay/BitNode change — purely `.claude/skills/` catalog work, same day as the BN4.2 replay-start session below but a separate, later session. A 5-agent parallel sweep covered all 14 project-local skills (2 new since the last 2026-08-10 audit: `diagnose-loop-bug`, `ram-costs-refresh`); 6 came back clean (`boot-chain`, `build-check`, `check-unlock`, `dev-watch`, `ns-cost-lookup`, `position-tail-window`). Fixed in 3 phases, all pushed (`3631a2c`, `e0160ec`, `37c2725`): **Phase 1** — 5 correctness bugs, incl. `activate-check`/`new-worker-script` both misdescribing real boot-chain/dispatch topology, and `diagnose-loop-bug`'s own hot-files list already drifted from the memory note it cites. **Phase 2** — extracted the `LAUNCH_BLOCK`/retry-constant template (previously hand-synced between `new-background-loop` and `reorder-chain-launch` via "keep both in sync" comments — the exact duplication a 2026-08-10 decision below had deliberately scaled back from) into a real shared asset (`new-background-loop/assets/chain-launch-block.ts`), this time verified via a working scratch-clone smoke test; collapsed the `ram-audit`/`ram-costs-refresh` excluded-namespace list triplication to point at `ram-costs.json`'s `__note__`; gave `ram-costs-refresh`'s destructive bulk-edit step a real `AskUserQuestion` gate. **Phase 3** — extracted `diagnose-loop-bug`'s naming-guess-then-fallback grep into `scripts/find-decision-function.sh`, verified live against a guess-hit (`controller.ts`) and two guess-misses (`faction-work-loop.ts`, and a newly-found second miss on `augment-loop.ts`). See the dated section below for the full write-up, including a real near-miss caught mid-verification.

**2026-08-16: BN4 cleared for real (SF4.1 obtained), and the save is now mid-replay in BN4.2 (2 more clears targeted for SF4.3).** No code changes this session — purely a BitNode-transition/play session, confirmed via `git status` clean and nothing unpushed. The one new finding: `crime-loop.ts`'s post-restart RAM-race fallback (fixed 2026-08-15) was re-verified against a **genuine fresh BitNode entry**, not just a mid-playthrough augment-install reset — the two reset home RAM differently (BitNode entry drops it to a bare 32GB; augment install leaves RAM/cores intact), so this was a real untested path. Confirmed self-resolving exactly as designed: at 32GB only the 4 unconditional scripts were resident, and `crime-loop.js`/`gang-manager.js`/`server-purchase-manager.js`/`program-buy-loop.js`/`hacknet-manager.js` all joined the instant `home-ram-loop.js`'s first purchase crossed the 64GB gate. See the dated section below and `[[bitburner_bn4_singularity]]` for detail. Because BN4.2 is a **fresh BitNode instance**, all in-playthrough state (karma, gang, augmentations) reset to zero again — the 2026-08-15 confirmations below (Territory Warfare, NFG-gate 4th fix, post-restart hand-off) are validated as *code*, but need to play out again in this new instance before they're re-confirmed as *live behavior* here. Also: dev watchers (`npm run watch`/`sync`) restarted this session, routine housekeeping.

**2026-08-15: two of the four unverified 2026-08-12/13 fixes are now confirmed live, a new post-restart crime bug found and fixed, and gang power growth via Territory Warfare implemented (not yet observed live).** `augment-loop.ts`'s NFG gate needed a 4th variant (`1855448`: gang-locked augmentations were still incrementing `gatedCount` even though they'd been excluded from `donateTarget`) before the symptom actually cleared — confirmed live afterward, alongside `faction-work-loop.ts`'s work-slot release (`bdec741`, from 08-13) working end-to-end. Separately, a fresh post-augment-install restart left the character idle — `decideActiveWorkScript` could only pick crime pre-gang, and `crime-loop.ts` had its own self-stop guard that would've killed it anyway even if selected — fixed (`a7bc3e4`) and confirmed live. New feature: `gang-manager.ts` now assigns trained members to the "Territory Warfare" task once respect is capped (`5252847`), researched from the game's own TypeScript source — the existing 65%/55% engage/disengage hysteresis was dead code because nothing ever grew gang power. **Not yet observed live.** `ef74360` (backdoor-loop allowlist) and `c308cad` (augment-loop donations), both from 08-12/13, also remain unconfirmed — see Known Issues.

**`augment-loop.ts`/`faction-work-loop.ts`: the NFG-only-install bug family now has its 3rd root cause found and fixed, plus two new automation features layered on top (2026-08-12/13) — see that section below for the full chain.** The 2026-08-11 rotation fix (below) turned out to be necessary but not sufficient: reputation was accumulating across factions as designed, but a second gate bug (`candidates.length === 0` also true for a rep-gated-but-real augmentation, not just "nothing left") kept dumping budget into NeuroFlux Governor anyway (confirmed via a follow-up screenshot: NFG level climbed 30→47 between the two bug reports). Fixed (`c029546`), then `faction-work-loop.ts` also picked up a fix for the case where the *only* joined faction has nothing left to buy from at all (`bdec741`), and `augment-loop.ts` gained a `donateToFaction` path to actively close rep gaps with spare money instead of leaving it idle (`c308cad`). All three, plus `backdoor-loop.ts`'s new 15-server allowlist (`ef74360`), compile clean but are **not yet confirmed against real in-game ticks** — see Known Issues/Next Steps.

**`augment-loop.ts`/`faction-work-loop.ts`: NFG-only install bug root-caused down to `faction-work-loop.ts` picking one faction forever — fixed 2026-08-11, since superseded by the 2026-08-12/13 fixes above (rotation itself held up; a second independent gate bug caused the symptom to recur).** User reported the augment purchaser kept installing NeuroFlux Governor batches with no real augmentation mixed in (screenshot evidence: "NeuroFlux Governor - 30" then "- 31", nothing else). First fix attempt (`c09d707`) assumed budget starvation — `buyNeuroFlux()` ran unconditionally with leftover budget every tick, so it could drain cash before a pricier real augment ever became affordable; gated NFG purchases on a real augment already being queued. **That fix didn't resolve the symptom** — a follow-up screenshot still showed NFG-only. Rather than guess again, added a diagnostic `ns.print` line to `augment-loop.ts`'s tail (`candidates=N gated=N queuedHasReal=... canBuyNfg=...`) and had the user paste the live tail output: `candidates=0 gated=60`, budget climbing past **$2.9 billion completely idle**. Money was never the bottleneck — 60 real augmentations exist and are unowned across the player's joined factions, all blocked by insufficient faction *reputation*. Root cause: `faction-work-loop.ts` picked one faction on first success and never revisited the choice (only re-searched if `workForFaction` outright failed) — every other joined faction's reputation stayed frozen indefinitely. **Fix (`0d3598f`):** `faction-work-loop.ts` rewritten to re-rank all joined factions every tick by reputation gap to their nearest unowned/non-NFG augmentation (`orderFactionsByAugmentGap`) and work whichever is closest, naturally rolling onto the next-closest faction as each gap closes to zero. `ram-audit` caught two real bugs in the new code before it ever synced: `Infinity - Infinity` producing `NaN` in the sort comparator, and a `??` operator tripping this repo's confirmed phantom-RAM-charge finding — both fixed pre-push. Also added the 4 new `singularity.*` RAM costs this needed to `ram-costs.json`. **Not yet confirmed in-game — user will check tomorrow**: whether `gated` trends down from 60, whether `faction-work-loop`'s tail shows target-switching lines, and whether the next install finally shows a real augmentation instead of NFG-only. `company-work-loop.ts` has the identical "pick once, stick forever" pattern (`targetCompany`) — not fixed or investigated this session, lower-stakes since company reputation doesn't gate augmentation purchases the way faction reputation does, but worth a look if a similar staleness symptom ever shows up there.

**`gang-manager.js`'s long-running RAM squeeze is root-caused and fixed (2026-08-10) — no longer a "not yet launching" caveat.** The 36.10GB monolithic script (flagged as a recurring, unfixed squeeze across the 2026-08-04-night/2026-08-09 notes below) is now a ~3.60GB orchestrator dispatching transient `gang-agent-*.ts` workers for the actual `ns.gang.*` calls — see the 2026-08-10 section below. Confirmed live: `mem scripts/gang-manager.js` = exactly 3.60GB, and `ps` shows every manager (gang/hacknet/darknet/server-purchase/home-ram/home-cores/program-buy/backdoor/crime) resident simultaneously alongside a full weaken/grow/hack dispatch.

**BitNode transition (2026-08-09, undocumented until now — happened in an earlier unclosed session before this write-up): BN5 (Intelligence) cleared for real (destroyed `w0r1d_d43m0n`, not flumed — SF5 permanent) → BN4 (Singularity, current), this save's unfinished business from the original BN4→BN2→BN3→BN5 chain below.** Per `[[bitburner_bitnode_route]]`'s researched order. Singularity automation revived same day (`c54ca5c` and follow-ups) — see the 2026-08-09 section below for the full pivot, the karma/gang/augment automation built this session, and a RAM-driven redesign of the whole Singularity work-loop group. `[[bitburner_singularity_locked]]`/`[[bitburner_bn4_singularity]]` memory already reflect this; this file and `[[bitburner_project_docs]]`'s index line did not, until now.

**Prior BitNode transition (since last close, 2026-07-30): BN4 (left early, uncleared) → BN2 (Gang, completed) → BN3 (Corporation, built + paused) → BN5 (Intelligence).** Later the same day the BN3 corp work below was completed, the user discovered they hadn't actually cleared BN5 yet (misremembered from an earlier planning session) and — per the researched BitNode order in `[[bitburner_bitnode_route]]` (SF5's Intelligence + permanent `Formulas.exe` are more valuable earlier in the run, and Formulas.exe would help a future BN3 corp revisit too) — left BN3 pre-augment via `b1t_flum3.exe` (no SF3 earned by design) to run BN5 first. `corp-manager.js`'s chain-launch was commented out (not deleted) in `controller.ts`, along with its RAM reservation, both marked `PAUSED (2026-08-04)` — the full BN3 build below is complete and stays intact for when BN3 is resumed, it just isn't running right now. Per `[[bitburner_bitnode_route]]`/`[[bitburner_bn3_corp]]` memory: SF1 (partial/uncleared BN4) + SF2 (BN2 cleared) in hand going into BN3; neither grants any corp-specific bonus (confirmed against `bitburner-src`).

**BN2 (Gang) automation — built and completed across several unclosed sessions (2026-07-30–08-02), never documented here until now.** `gang-manager.ts` was scaffolded (`47efbe7`) implementing the researched recruit → train/ascend → equip → territory-warfare loop (`[[bitburner_bn2_gang]]`). Fixed across follow-ups: members stuck idle because the game's own "Unassigned" task false-matched the train-task picker (`9ebd875`); "Train Combat"/"Train Hacking" ambiguously flagged both `isCombat`/`isHacking` in game data, fixed by matching task name directly (`54e74f3`); earn-task scoring switched from raw `baseMoney`/`baseRespect` to a formula-derived, stat/territory-weighted score (`220dcb1`); earn tasks switch from respect to money once respect passes 3,500,000, converted at the real 75:1 respect→reputation ratio, not 1:1 (`ed019e9`/`0da00c7`). **BN2 confirmed complete** (user-confirmed 2026-08-03).

**RAM-analyzer phantom-charge bug (bare property names colliding with real `ns.*` method names) confirmed more broadly and automated** (`0d4a7c5`, 2026-07-30) — e.g. `member.hack` or a locally-typed field named `weaken`/`grow`/`hack` gets phantom-charged as if that method were called. Fixed 4 collisions in `gang-manager.ts` (bracket notation — the colliding names are the Gang API's own fields) and several in `controller.ts`/`battlestation.ts` (renamed local fields). `ram-audit.mjs` now auto-detects this shape. **This exact bug class recurred in this session's new BN3 corp code** (see below) — `ram-audit` doesn't cover the Corporation API at all, so it can't auto-catch it there; live `mem` is the only reliable check for `corp-agent-*.ts`/`corp-manager.ts` going forward. `[[bitburner_ram_analyzer_bugs]]` updated with both findings.

**Chain-launch simplified: every Singularity-gated script dropped from the boot chain** (`f7c0968`, `b9b614a`, prior sessions) — `home-upgrade-loop.ts`, `backdoor-loop.ts`, `company-work-loop.ts` all confirmed to hard-error without SF4 (this save never cleared BN4 to SF4.3), so they're removed entirely rather than sitting gated-forever. `battlestation.ts` also removed from managed chain-launch (manual-only now). Lower-priority manager launches (`gang-manager.ts`/`hacknet-manager.ts`/`darknet-manager.ts`/`server-purchase-manager.ts`) consolidated into `controller.ts`'s own dispatch-loop tick (`184b674`), each reserving its own resident RAM cost once eligible and not yet running (`d61ab882`) — the same reservation pattern this session's new `computeCorpReserveGb` (planned for Step 9, see below) directly extends.

**New tooling**: `build-check` skill (formalizes `npm run build` + dev-watch liveness verification) and `boot-chain` skill (prints the chain-launch tree with per-script RAM costs) — both added 2026-07-30.

**Hacking automation chain (architecture — still live in the codebase; corp's launch block is currently the only one commented out, everything else below is running):** self-assembling, no dedicated launcher. `scan-root.ts` (recon+root) → `rescan-loop.ts` (re-scans every 30s) → `controller.ts` (two-phase prep-then-HWGW-batch weaken/grow/hack dispatch; `getHostPool` = `home` + `ns.cloud.*` purchased servers) → from inside `controller.ts`'s own dispatch loop: `server-purchase-manager.ts` (unconditional once `rescan-loop.ts` is up), then — once `hasEnoughHomeRam(64GB)` passes — `gang-manager.ts`/`hacknet-manager.ts`/`darknet-manager.ts`, now **tiered, not flat** (see the BN5 section below): `hacknet-manager.ts`/`darknet-manager.ts` are only even attempted once `gang-manager.ts` is confirmed running. `src/lib/launch.ts` holds `hasEnoughHomeRam`/`runWithRetry`; `src/lib/network.ts`/`root.ts` hold recon/root logic. Darknet tail (`darknet-manager.ts` + 3 dispatched `ns.dnet` workers) is self-learning, state in `/data/darknet-kb.json`.

---

### 2026-08-04: BitNode 3 Corporation automation — "Bosko Industries" — **v1 build complete, all 9 steps live-verified — PAUSED same day, see BN5 section below**

Scoped via a full `/interview` (Project Brief confirmed, independently reviewed by a subagent — caught a real blocking gap: missing Office API/Warehouse API/Smart Supply unlock prerequisites) and a Plan-Mode implementation plan (`/home/bosko/.claude/plans/joyful-tickling-nygaard.md`, itself validated by a Plan subagent before approval — corrected a real misconception about per-loop-iteration RAM cost). Built incrementally across two sessions, one step at a time, each verified live in-game before the next started.

**Architecture**: `corp-manager.ts` is a near-0GB orchestrator (3.60GB resident — confirmed via live `mem`), state-driven via `ns.corporation.nextUpdate()` (0GB, acts only on `prevState === "START"`), dispatching short-lived single-purpose `corp-agent-*.ts` worker scripts via `ns.exec` for the actual GB-costed `ns.corporation.*` calls (most cost 10-20GB each) — mirrors `darknet-manager.ts`/`darknet-agent-*.ts`, but file-based state (`/data/corp-*.json`, one sole writer per file) instead of port-based, since corp workers never leave `home` (no remote-host traversal like darknet's hop-walking). 16 worker scripts + `corp-manager.ts` + `src/lib/corp-constants.ts` (shared constants/types — `CityName`/`CorpEmployeePosition`/etc. derived via `NS["corporation"]["<method>"]` since `NetscriptDefinitions.d.ts` doesn't export those type names directly, same convention as `gang-manager.ts`'s `GangInfo`). `src/lib/types.ts` extended with 6 new report interfaces.

**Build progress (9-step plan, all steps live-verified):**
- ✅ Step 1 — `corp-agent-create.ts` + bootstrap loop: "Bosko Industries" founded via free BN3 seed funding. Confirmed live.
- ✅ Step 2 — unlocks: Office API, Warehouse API, Smart Supply all purchased. Confirmed live.
- ✅ Step 3 — division founded, industry data cached: confirmed live Agriculture is materials-only (`makesProducts=false`, `makesMaterials=true`), requires Water+Chemicals, produces Plants+Food — matching `[[bitburner_bn3_corp]]`'s research exactly.
- ✅ Step 4 — expanded to all 6 cities (Sector-12 pre-existing as HQ). Confirmed live.
- ✅ Step 5 — warehouses + smart supply. Confirmed live, but **two real bugs found and fixed en route**: (1) missing funds check before `purchaseWarehouse` (~$5B each) — `corp-manager.ts` now checks `getConstants().warehouseInitialCost` (0GB) first; (2) **write actions were re-dispatching against stale cached state** — confirmed live (`purchaseWarehouse` fired twice for all 5 cities, possibly double-charging ~$25B) — fixed with `dispatchWriteAction()`, which `ns.rm()`s the report a write action consumed immediately after success, forcing a fresh status re-check before the same action can fire again. Now used for every write action.
- ✅ Step 6 — offices staffed (Operations/Engineer/Business × 6 cities = 18 hires via `hireEmployee`'s `position` arg — no separate `setJobAssignment` needed). Confirmed live.
- ✅ Step 7 — sell orders + materials status. Root-caused a **livelock**: the queue dispatches one worker per `"START"` tick, and by Step 7 there are 5 sequential refresh gates (core → unlocks → offices → warehouses → materials) ahead of the sell-order check; with thresholds clustered around 30-60s and real corp ticks slower than assumed, a full traversal could exceed the *earliest* gate's own threshold, so `core` went stale again before the chain ever reached `materials`, looping forever. Fixed by loosening every threshold well past plausible full-traversal time (`CORE_REFRESH_MS` 30s→120s, `UNLOCKS_REFRESH_MS` 60s→300s, `OFFICES`/`WAREHOUSES`/`MATERIALS_REFRESH_MS` 30s→60s), plus success-path `ns.print` on `dispatchOnce`/`dispatchWriteAction` (previously silent on success — the thing that made this livelock hard to diagnose at all). **Reconfirmed live this session** — decisively, not just from the UI: `corp-manager.ts`'s idle message only prints once `needsSellSetup` evaluates false, which structurally requires every producible material at `desiredSellAmount === "MAX"`. UI backed it up: Food/Plants `Sell 3.4xx/MAX` in Sector-12, **Profit flipped from -$10k/sec to +$105.442k/sec** — the core definition-of-done evidence for the whole subsystem.
- ✅ Step 8 — morale/energy steady state (`corp-agent-buy-tea.ts`/`corp-agent-throw-party.ts`). Reuses the `offices` report already fetched for staffing rather than a separate steady-state tick-counter (the existing staleness-gated-report mechanism already bounds dispatch frequency the same way). Confirmed live: new idle message (`...selling+morale-steady...`) shows both branches evaluate cleanly against real data — energy/morale were already at 100% (above the 90% threshold) so nothing dispatched yet; the actual `buyTea`/`throwParty` firing itself remains untested until real production time decays them below threshold, worth a spot-check later. `mem`-verified clean RAM cost (21.60GB each, no phantom-charge collision).
- ✅ Step 9 — wired into `controller.ts`: `computeCorpReserveGb` reserves `corp-manager.js`'s resident cost + the single largest `corp-agent-*.js` worker's cost, capped at 50% of home max RAM (`CORP_RAM_RESERVE_FRACTION`), folded into `currentReserveGb` unconditionally. Launch block added just before the `server-purchase-manager.js` block: unconditional once `rescan-loop.js` is up, **no** `hasEnoughHomeRam` gate (corp supersedes hacking, per user direction — see Recent Decisions). `activate-check` confirmed all 16 `corp-agent-*.js` workers + `corp-manager.js` reachable from the chain. Verified live after a full cold restart (`killall` + `run scan-root.js`): `corp-manager.js` resident in `ps` and progressing; weaken/grow/hack dispatch still gets 75-95GB genuine leftover RAM from the pool across ticks, multiple real targets admitted and funded.

**Confirmed live this session (2026-08-03/04)**: the RAM-analyzer bare-property-name collision recurs on the Corporation API — `corp-manager.ts` was phantom-charged 10GB for `w.hasWarehouse` (a plain JSON field) colliding with real `ns.corporation.hasWarehouse()`. Fixed by renaming the field to `warehouseExists`.

**Live-diagnosed during Step 9 verification**: `hacknet-manager.js` (7.60GB) can't launch on this save — home is 64.00GB total, 63.15GB used, 0.85GB free. `mem`-verified this is **not** a Step 9 regression: pre-corp headroom was already only 4.45GB (short of hacknet's 7.60GB need by ~3.15GB) even without `corp-manager.js`'s 3.60GB resident cost. `gang-manager.js` alone is the dominant consumer at 36.10GB — over half of home's entire budget. Since home RAM can't grow on this save (`home-upgrade-loop.ts` confirmed SF4-gated, hard-errors without it), this is a durable condition, not a transient squeeze — noted for awareness, not currently blocking anything (hacknet income is minor next to corp's).

**v1 (Agriculture, single division, hands-off) is now complete end-to-end.** The confirmed brief's out-of-scope list (2nd division, R&D, advertising, IPO, faction bribing) is the natural v2 backlog — not scoped yet, and now on hold until BN3 is resumed (see below).

---

### 2026-08-04 (night): BitNode 3 → BitNode 5 pivot, plus a real gang-manager RAM-priority fix

**The pivot.** Same day the BN3 build above finished, the user asked when Intelligence unlocks and realized they hadn't actually cleared BN5 yet — a misremembering from an earlier BitNode-order planning session, not a deliberate skip. Decided on the spot to abandon BN3 for now and run BN5 first, matching `[[bitburner_bitnode_route]]`'s researched order. Left BN3 pre-augment via `b1t_flum3.exe` (no SF3 earned — that's expected, the flume skips `giveSourceFile`, this wasn't meant to clear BN3 for real). `corp-manager.js`'s launch in `controller.ts`'s `main()` and its `+ computeCorpReserveGb(ns)` term in `currentReserveGb` are both commented out (not deleted) and marked `PAUSED (2026-08-04)` — `npm run build` still exits 0 with them commented, `computeCorpReserveGb` itself is now flagged unused by `tsc` (harmless, expected while paused).

**Also added this session: `karma.ts`**, a one-line diagnostic (`ns.tprint(\`karma: ${ns.getPlayer().karma...}\`)`) — karma isn't shown anywhere in the game UI, and `ns.getPlayer().karma` is simpler than the Singularity-gated `ns.heart.break()` alternative `gang-manager.ts` deliberately avoids. Not wired into the chain; run by hand (`run karma.js`) whenever wanted. Didn't fit `new-worker-script`'s template (that skill only generates a single bare `await ns.<method>()`, not a two-call `tprint(getPlayer()...)` diagnostic), so hand-written matching repo conventions instead.

**BN5 research** (`[[bitburner_bn5_intelligence]]`, 7 sources, 4/7 substantively on-topic): no BN5-specific win condition or script changes needed — same root→weaken/grow/hack→destroy-the-node loop, must actually destroy `w0r1d_d43m0n` for real this time (flume would forfeit SF5 the same way it forfeited SF3 in BN3). SF5 pays off permanently: the Intelligence stat itself (never resets on future BitNode transitions), `Formulas.exe` + `ns.getBitNodeMultipliers()` (useful for a future BN3 corp revisit too), and a permanent hacking-multiplier boost (8% at SF5.1, 14% at SF5.3 — target .3 if time allows, it's a hard cap). Consensus: don't build dedicated Intelligence-farming automation — gains are extremely slow via mostly-hidden mechanics; treat it as a passive byproduct of normal play (biggest single chunk, ~300 XP, lands when the node is actually destroyed; `SQLInject.exe` creation is the best *active* lever at ~2,800 XP if not already bought). **Practical upshot for this save: just keep playing as normal.** Gang isn't new work either — SF2 (earned for real completing BN2) makes the Gang API available in any BitNode subject to the same karma ≤ -54,000 threshold, and `gang-manager.js` was already live in the chain untouched by the BN3 pause.

**The RAM-priority bug this pivot immediately surfaced.** Flagged as a non-blocking caveat during the BN5 planning chat (before this fix): the known `gang-manager.js`-vs-`hacknet-manager.js` RAM squeeze (`[[bitburner_corp_hacknet_ram_squeeze]]`, confirmed during BN3 Step 9) would likely resurface in BN5 since home RAM hasn't grown (no SF4, no scripted upgrades). It did — user reported `gang-manager.js` (36.10GB) simply wasn't running (absent from Active Scripts) while `hacknet-manager.js`/`darknet-manager.js` were. Root cause: `controller.ts`'s lower-priority-manager launch loop tried `gang-manager.js` first each tick, but that's only *attempt*-order — `ns.run()` checks real free home RAM independently, so when free RAM was too small for gang's 36.10GB but big enough for hacknet+darknet's combined 10.6GB, the smaller two launched anyway on the same tick and then sat resident forever, permanently locking in RAM gang still needed. **Fix (`9a44b97`):** `controller.ts`'s launch block now gates `hacknet-manager.js`/`darknet-manager.js`'s launch *attempt* itself on `gang-manager.js` already being confirmed running (`ns.isRunning`), not just tried-first — they can no longer grab RAM out from under it. Live-verified: since hacknet/darknet were already resident and don't self-evict, the user killed both once in-game so the new code could hold their RAM for gang instead; user confirmed gang-manager.js came up after (**"that seemed to fix it"**). Long-term durability (does it stay up across future restarts without a manual nudge) isn't separately proven yet — reasonable to expect given the fix is structural, not a one-off.

---

### 2026-08-06: `home-upgrade-loop.ts` split + a second RAM-starvation bug found and fixed (`scan-root.js`) — both confirmed live

Two independent fixes, no BitNode/architecture change — still BN5 (Intelligence), corp still paused per the pivot above.

**Split `home-upgrade-loop.ts` into `home-ram-loop.ts`/`home-cores-loop.ts`** (`fd2e9df`) — each now loops a single `singularity.upgradeHomeRam()`/`upgradeHomeCores()` call instead of both combined, so a manual run only pays RAM for the one upgrade actually wanted (roughly half the old script's confirmed 99.65GB combined cost — see `[[bitburner_singularity_locked]]`). Neither is chain-launched (both still hard-require SF4, unavailable this save) — purely a manual-run RAM-efficiency improvement. Side effect: `computeCorpReserveGb` (plus its supporting `CORP_MANAGER_SCRIPT`/`CORP_WORKER_SCRIPTS`/`CORP_RAM_RESERVE_FRACTION` constants) had gone fully unused once the corp launch block was commented out during the BN3→BN5 pivot — commented out under the same `PAUSED (2026-08-06)` marker so it re-enables in one place alongside the launch block when BN3 resumes.

**Diagnosed and fixed a second, distinct RAM-starvation bug: `scan-root.js`** (`67f3328`) — user reported many hosts stuck `[locked]` in `server-tree.js` despite owning all 5 port openers (confirmed present on home) and sufficient hacking level (irrelevant to root access anyway; only ports gate `ns.nuke`). Root cause: `controller.ts`'s `currentReserveGb()` reserved RAM for every *persistent* manager's own launch (rescan-loop, server-purchase-manager, gang/hacknet/darknet), but never for `scan-root.js` — the transient 3.80GB script `rescan-loop.js` spawns fresh every ~30s while staying resident itself, so `rescan-loop.js`'s own reserve term was always zeroed by the existing double-counting guard and nothing covered the recurring child spawn. Dispatch's greedy weaken/grow/hack RAM claim left ~0 free almost every tick, so the spawn silently failed ~4/5 cycles — confirmed live via `tail rescan-loop.js` showing repeated "Cannot run scripts/scan-root.js... requires 3.80GB." **Fix:** added a `scanRootReserveGb` term to `currentReserveGb()`, same `isRunning`-gated pattern as the other reserves. **Confirmed fixed in-game the same session** — user reported the RAM-failure spam stopped after the fix synced ("that fixed it"). See `[[bitburner_scan_root_ram_starvation]]`, cross-linked with the sibling `[[bitburner_corp_hacknet_ram_squeeze]]` fix — same bug class (greedy dispatch starving a lower-priority script of home RAM), different code path: that one covered a persistent manager's own resident cost, this one covers a persistent loop's *transient child spawn*.

---

### 2026-08-07/08: No code changes — SF4-gated program-buying question answered, dev watchers restarted

Two brief sessions, neither touched code (`git status` clean, no commits since the 08-06 close).

**2026-08-07** — user asked whether a script could buy dark-web programs (`FTPCrack.exe`, `SQLInject.exe`, etc.) without SF4. Confirmed a hard no: `ns.singularity.purchaseProgram()` is the only scriptable equivalent of the terminal `buy` command, and it lives entirely inside the SF4-gated `Singularity` interface — this save left BN4 early (uncleared) and has never earned any SF4 level since. Offered to write the script now anyway so it's ready to drop in the moment SF4 exists; the user hadn't decided by session end (open, not actioned — see Next Steps). Also prompted an update to `[[bitburner_singularity_locked]]`: its 2026-07-26 "lock lifted inside BN4" note only ever applied while the save was actually in BN4, and the save has since moved on to BN2→BN3→BN5 with the lock back in full force — memory updated to flag that reversion explicitly.

**2026-08-08** — `npm run watch`/`npm run sync` restarted as detached background processes via the `dev-watch` skill (routine dev-loop housekeeping, no code changes).

---

**Historical (BitNode 4, superseded — save is no longer in this BitNode):** a 6-session, 6-root-cause "$0/pitiful scripted income" saga on the fresh-BN4 hacking chain, fully resolved (confirmed live: `hack.js` running, money climbing) before the save moved on. Root causes in order found: `buildWorkingSet`'s debit-by-`needed`/`break`-vs-`continue` bug (`c344ad9`) → purchased-server RAM fragmentation from a buy/upgrade tie (`f2ccfc8`) → `rescan-loop.js` dying with nothing to relaunch it (`7c666e0` watchdog) → `buildWorkingSet`'s 1-thread admission bar diluting a small fleet too thin (`20a2872`) → one due target's uncapped grow request starving every other target each tick (`75bae6a`) → fleet-growth managers themselves starved of RAM plus three more stacked dispatch bugs (`47b60fe`). Also built: self-learning `ns.dnet` darknet automation, `bitnodes.md`/`jobs.md`/`singularity-roadmap.md` research docs, and the RAM-priority gating system (`hasEnoughHomeRam`, 64GB threshold) this session's BN3 work extends. Full detail recoverable from prior commit history / `session-summary-archive.md` if BN4 is ever replayed to actually clear it to SF4.3.

---

### 2026-08-09: BitNode 5 → BitNode 4 pivot, Singularity automation revived, new karma/gang/augment automation, and a RAM-driven redesign of the whole work-loop group

**The pivot (happened in an earlier unclosed session today, before this write-up — backfilled from git log + memory, same gap pattern flagged in prior closes).** BN5 cleared for real (SF5 permanent, `Formulas.exe` free). Per `[[bitburner_bitnode_route]]`'s order, moved to BN4 next. Researched a BN4 kickoff strategy via `/research` (10 sources — see `[[bitburner_bn4_singularity]]`): lean into full Singularity automation immediately since it's native-cost while physically inside BN4 regardless of SF4 level; reaching SF4.3 needs 3 real clears (no flume credit).

**Singularity automation revived (`c54ca5c`).** Re-wired `home-ram-loop.ts`/`home-cores-loop.ts`/`company-work-loop.ts`/`backdoor-loop.ts` (all 4 dropped from the chain 2026-07-31 when Singularity appeared hard-locked even inside BN4) back into `controller.ts`, plus a new `faction-work-loop.ts` (the roadmap's missing Tier 3). Confirmed live: the 07-31 anomaly does **not** reproduce — all 5 ran cleanly at native RAM cost, no SF4 errors. The one real failure found (`backdoor-loop.js` erroring on the player's own purchased servers) was an unrelated pre-existing bug, fixed by filtering `purchasedByPlayer` hosts (`31aa512`). Also added `program-buy-loop.ts` (`661925e`) after confirming nothing bought the TOR router/port-openers. `[[bitburner_singularity_locked]]`/`[[bitburner_bn4_singularity]]` have the full resolution writeup.

**New this session: karma-grind-to-gang automation + augmentation automation.** Scoped via `/interview`: mug until Homicide's success chance clears 50%, then Homicide-only until the gang exists; after that, faction/augments outrank company work. `gang-manager.ts` (built during the BN2 arc) already handles founding once karma/faction conditions are met — the only missing piece was the crime automation to actually drop karma. Built:
- `crime-loop.ts` — Mug until Homicide chance ≥50%, then Homicide on repeat; returns (self-terminates) once `ns.gang.inGang()`.
- `augment-loop.ts` — gated on gang existing (avoids resetting crime-relevant stats mid-grind via a premature install). Buys rep/money-eligible augmentations priciest-first (minimizes total cost against the game's per-purchase price multiplier — a known exchange-argument result, not guessed), spends leftover budget on NeuroFlux Governor last, then `installAugmentations("scripts/scan-root.js")` once a batch is queued and nothing else is buyable. The `cbScript` re-triggers the entire chain-launch bootstrap after the reset — `program-buy-loop.js` re-buys whatever `.exe` programs the reset wiped, automatically. This is the "re-bootstrap handling" `singularity-roadmap.md` had flagged as the blocker on this exact automation.

**RAM-driven redesign, found via live testing, not assumed.** The first version kept `crime-loop.ts`/`faction-work-loop.ts`/`company-work-loop.ts` all resident simultaneously, coordinating the shared Singularity work slot via internal `getCurrentWork()` deference checks each script ran on itself. Live `mem`+`ps` showed this doesn't work: `company-work-loop.js` (8.10GB) + `faction-work-loop.js` (11.60GB) both stayed resident, leaving ~6GB free — not enough for `crime-loop.js`'s 11.60GB, so it silently never launched. **User-directed fix: only one of the three should ever be resident.** Key insight: Bitburner charges RAM once per distinct `ns.*` function *referenced* in a script, regardless of call count — so merging all three into one file would cost a constant ~27.6GB always, while keeping them separate and switching which one runs costs only whichever one is active (8.10–11.60GB). Moved the decision into `controller.ts` (`decideActiveWorkScript`, +1.0GB: `ns.kill` and `ns.singularity.getCurrentWork`, confirmed live at exactly 12.50GB). Behavior: `crime-loop.js` exclusively pre-gang; post-gang it's killed **permanently** (not idled), `augment-loop.js` runs unconditionally (doesn't touch the work slot), and faction/company are mutually exclusive — faction preferred, falls back to company after a 90s grace period if `getCurrentWork()` shows no real faction work happening, re-probes faction every 5 minutes while on company. Confirmed live: `ps` shows only `crime-loop.js` resident pre-gang (not faction/company), `mem controller.js` = 12.50GB exactly as predicted, `tail crime-loop.js` actively committing Homicide with no errors.

**A second real bug, found by user report rather than testing:** `commitCrime(crime, true)` (focus=true, the API's own default) forced the game to jump to the Work screen on every call — since Homicide's duration is only ~3s, the screen was yanked back every few seconds, making the UI unusable. Fixed by switching to `focus: false`, matching the convention `faction-work-loop.ts`/`company-work-loop.ts` already used (pre-existing in this repo, not something this session introduced — `crime-loop.ts` was just the one new file written with the wrong default). Confirmed live: no more screen-jumping after the fix.

**Not yet observed live:** actual gang creation (karma needs to reach ≤-54,000, a long real-time grind) and actual augmentation purchases (needs real faction rep to accumulate). Both are structurally verified (build + `activate-check` + live `tail`/`mem`/`ps` of the mechanism itself) but not watched end-to-end yet.

**Live-observed, not investigated this session (pre-existing, not a regression):** `gang-manager.js` (36.10GB) is absent from `ps` on this BN4 save too — same RAM-squeeze class as `[[bitburner_corp_hacknet_ram_squeeze]]`/the BN5-era fix, just recurring in a new context now that `backdoor-loop.js` (35.90GB) and the whole Singularity work-loop group are also competing for home RAM. Not investigated or fixed this session — flagged in Known Issues below.

---

### 2026-08-10: `gang-manager.js` RAM starvation root-caused and fixed — reprioritized, then rebuilt as a cheap orchestrator + worker scripts

User-directed, iterative: three narrower fixes were tried first and each turned out insufficient before the real root cause was found, all in one session — worth keeping the sequence, not just the final state.

**Attempt 1 — reorder launch-attempt order.** User asked for `gang-manager.js` to launch right after `crime-loop.js` and ahead of `backdoor-loop.js`/`program-buy-loop.js`/`home-cores-loop.js` in `controller.ts`'s per-tick launch sequence (`home-ram-loop.js` kept its existing first-crack priority, user's explicit choice). Built cleanly, but didn't fix it — `ps` still showed the three siblings resident and `gang-manager.js` absent.

**Attempt 2 — a `gangReserveGb` gate.** Root cause of attempt 1's failure: those three siblings' plain `ns.run()` calls have zero awareness of `currentReserveGb`'s internal reservation bookkeeping — that reserve only throttles the weaken/grow/hack dispatch loop, never consulted by sibling managers' own launch attempts. Added a live-free-RAM check before each of the three launches, holding them back if launching would leave `gang-manager.js`'s cost (36.10GB at the time) unfundable. Confirmed via `free`: shortfall closed from ~30.5GB to ~3.55GB, but still short — this only gates *future* launches, doesn't evict what's already resident (a deliberate choice, not an oversight — see Recent Decisions).

**Attempt 3 — push `server-purchase-manager.js` back too.** Same gate extended to `server-purchase-manager.js`, moved to launch right after `gang-manager.js`/hacknet/darknet, ahead of the other three. Shortfall narrowed further (~3.55GB free of the needed 36.10GB) but still short — at this point it was clear no amount of reordering/reservation could manufacture RAM that a 64GB home simply didn't have alongside `backdoor-loop.js` (35.90GB) and everything else.

**The real fix — split `gang-manager.ts` into a cheap orchestrator + transient workers**, mirroring the existing `corp-manager.ts`/`corp-agent-*.ts` pattern (offered as one of 4 options, user picked this one over live-eviction/wait-it-out alternatives — see Recent Decisions). `gang-agent-status.ts` now does almost every `ns.gang.*` **read** (bears ~21.10GB of static cost but exits in milliseconds, writing a cached `/data/gang-state.json` report — schema in `src/lib/types.ts`'s `GangStateReport`). `gang-manager.ts` itself is now pure computation over that cached report (ported `earnScore`/`pickEarnTask`/`pickTrainTask`/equipment-budget/warfare-hysteresis logic, none of it touching `ns`), dispatching one of 6 new single-purpose `gang-agent-{found,recruit,ascend,assign-task,buy-equipment,warfare}.ts` workers (3.1–5.6GB each) only when a decision actually requires a live call. Hit the same bare-property-name RAM-analyzer collision this repo has seen before (`member.hack`/`info.respectForNextRecruit`) — since the new report type is locally-controlled (not a game-defined type), fixed by renaming fields (`hackSkill`, `respectForNextRecruitThreshold`) rather than bracket-notation, cleaner for new code.

**Confirmed live, twice:** `ram-audit` predicted `gang-manager.ts` at 3.60GB pre-sync; `mem scripts/gang-manager.js` in-game matched exactly (1.60 baseCost + 1.30 exec + 0.60 rm + 0.10 fileExists — `ns.gang.inGang()` doesn't even appear, confirming it's genuinely free as documented). `ps` afterward showed every manager — gang/hacknet/darknet/server-purchase/home-ram/home-cores/program-buy/backdoor/crime — resident simultaneously, plus a full weaken/grow/hack batch spread across a dozen targets on `home` itself.

**Not yet separately verified:** actual gang activity progressing (respect/members climbing) over a longer run, beyond the one-shot `mem`/`ps` confirmation — the orchestrator's dispatch-log prints were pointed out as the way to check next session. Also not yet tested: a full cold restart of this new architecture (though at 3.60GB resident, the failure class the old 36.10GB version was prone to is structurally far less likely to recur).

Committed as `4731579` (single commit — reorder, gate, and the RAM split all landed in the same session before push).

---

### 2026-08-10 (late): Claude skill-infrastructure session — `backdoor-loop.ts` safety net, skill-suggestion/skill-upgrade run, 4-phase skill-audit sweep

No gameplay/BitNode change — this session was entirely about the `.claude/skills/` catalog (plus one small game-code fix). Three back-to-back tasks, same evening.

**`backdoor-loop.ts` `returnHome()` safety net (`be3b41e`).** User asked for the backdoor loop to return to `home` once done with a pass. Investigation found it mostly already did this — `installBackdoorOn()` already round-trips home→target→home for every successfully-backdoored host — but if `installBackdoor()` throws mid-pass (the `singularityUnavailable` branch), the return-trip `connect()` calls never ran, stranding the script off-home and breaking the next pass's home-rooted path hops. Added a `returnHome(ns, parents)` helper called unconditionally at the end of every pass (harmless no-op on a clean pass, closes the gap on an early bail). Verified via `npm run build` (PASS) and confirmed the compiled/synced file changed; the actual `singularityUnavailable` mid-pass branch wasn't observed live this session (hard to force on demand) — see Known Issues.

**`skill-suggestion`/`skill-upgrade` run.** Found and fixed the long-suspected root cause behind a Gotcha repeated in several skills' docs: `find-last-skill-invocation.sh` only detects assistant-initiated `Skill` tool_use entries, so a user-typed `/skill-name` (which Claude Code injects as user-turn text instead) is invisible to it — confirmed with real transcript evidence, not just theorized. **Fixed in the NixOS dotfiles repo** (this is a global, home-symlinked skill, not project-local) to also match `<command-name>/<skill></command-name>` user-turn text — staged there, **needs a NixOS rebuild + reboot before it's live in `~/.claude`** (see Known Issues). Also fixed in dotfiles: `skill-upgrade`'s Step 2 now checks project-local `.claude/skills/` before assuming a flagged skill is global (it had mis-resolved `new-background-loop`'s path this way), and a new global `CLAUDE.md` section documents the `rtk find` no-compound-predicates limit (previously only in bitburner-project memory, recurring across many skills/projects). In-repo: fixed `new-worker-script/SKILL.md`'s wrong script path (folded into skill-audit's Phase 1 commit below). Proposed and drafted one new skill, **`resume-session`** (locate+summarize `project-state.md`/`session-summary.md` at session start) — written to this session's scratchpad only, **not installed anywhere yet** (see Known Issues/Next Steps).

**Skill-audit — full 12-skill sweep, 4-phase fix-it pass, all pushed (`74c5b42..89385e6`).** Partitioned all 12 project-local skills into 3 disjoint background sub-agent groups against the standard rubric (bucket fit, deterministic→scripts, templates→assets, re-entered config, AskUserQuestion, Arguments section); every finding verified empirically (ran the scripts, grepped live source, checked `boot-chain`/`git log`) before being reported, per the skill's own bar. Six of twelve skills (`dev-watch`, `activate-check`, `boot-chain`, `build-check`, `position-tail-window`, `ram-audit`) came back clean — `ram-audit`/`boot-chain` singled out as the model to imitate (single source-of-truth cost table cross-imported, not copy-pasted). Fixed in 4 commits:
- **Phase 1** (`9bf7de5`) — correctness bugs: bare/stale `scripts/...` paths (404 from repo root) fixed to full `.claude/skills/<name>/scripts/...` paths in `secret-scan`, `new-background-loop` (its `## Scripts`/`## Assets` catalog lines only — Steps/Gotchas had already been fixed in an earlier pass), `new-worker-script`, `reorder-chain-launch`; dead unused `NETWORK_FILE` var (also factually wrong — port-opener logic is in `root.ts`, not `network.ts`) removed from `check-unlock.sh`; `new-background-loop`'s stale claim that `battlestation.ts` is still wired into `controller.ts` corrected (removed there in `83c833c`, independently corroborated by `activate-check`'s live run).
- **Phase 2** (`41b4a07`) — synced `reorder-chain-launch`'s Step 4 launch-block example against `scaffold-loop.sh`'s generated `LAUNCH_BLOCK` (they'd already drifted: `pid` vs `nextPid` naming, a comment present in one but not the other) plus their duplicated `LAUNCH_RETRY_ATTEMPTS=5`/`LAUNCH_RETRY_DELAY_MS=3000` constants — synced to match exactly with cross-reference comments in both, rather than a runtime-shared template (judged too risky given `scaffold-loop.sh`'s documented sed-corruption history for a duplication that was cosmetic, not yet a functional bug — see Recent Decisions).
- **Phase 3** (`a745430`) — added `ns-cost-lookup`'s missing `## Arguments` section (the only one of its group without one); converted two prose yes/no prompts to `AskUserQuestion` (`ns-cost-lookup`'s "add missing costs?" and `reorder-chain-launch`'s "default vs custom insertion position?").
- **Phase 4** (`89385e6`) — consolidated `new-background-loop`'s Gotchas: 3 pairs (6 bullets) chronicling successive rounds of the same now-resolved `scaffold-loop.sh` sed-corruption bug collapsed into 1 pair (2 bullets), user-confirmed wording via `AskUserQuestion` before applying.

`backdoor-loop.ts`'s `returnHome()` fix was committed separately (`be3b41e`, between Phase 1 and Phase 2) per user request, kept out of the skill-audit commits. All 5 commits pushed together at session end.

---

### 2026-08-12/13: server-tree BACKDOORED marker, 3rd NFG-gate root cause, backdoor-loop allowlist, faction work-slot release, augment-loop donations, two new skills

Still BN4 (Singularity). Five game-code commits plus two new project-local skills, all user-directed off real in-game reports/requests — no BitNode change.

**`server-tree.ts` shows `BACKDOORED` instead of req lvl once backdoored (`80822f4`, 2026-08-12).** Small, user-requested display fix — a rooted host that's already backdoored no longer shows a now-useless "req lvl" figure.

**NFG-only install bug, 3rd root cause (`c029546`).** User reported the symptom recurring (screenshot: NeuroFlux Governor level climbed 30→47 since the 08-11 fix). Root cause was a second, independent gap in the same `canBuyNfg` gate the 08-11 rotation fix didn't touch: `collectCandidates()` filters out rep-gated augmentations, so `candidates.length === 0` was true both for "nothing real left, ever" (the intended meaning) *and* "a real augmentation exists but hasn't hit its faction-rep threshold yet" — the loop treated both as license to dump the whole budget into NFG. The `gatedCount` (rep-gated real augs) diagnostic already existed for logging but was never wired into the actual decision; now it is (`canBuyNfg` also requires `gatedCount === 0`). Confirms the 08-11 rotation fix (`0d3598f`) itself was working as designed — reputation was genuinely accumulating across factions — this was a distinct bug on a different axis (money vs. rep, now both axes covered). Build clean; only "first positive signal" observed live, not the multi-day confirmation the first two variants needed — see Known Issues. Memory note `[[bitburner_augment_loop_nfg_gate]]` updated with the pattern for a possible 3rd variant.

**`backdoor-loop.ts` now only targets 15 servers that matter (`ef74360`), not every rooted host.** User asked to stop wasting cycles backdooring servers where it's a pure no-op (n00dles, foodnstuff, etc.); requested `/research` if the exact required list wasn't already known. Researched and cross-checked against 3 independent sources (Bitburner's own docs + 2 community automation projects): 5 hacking-group faction servers (`CSEC`/`avmnite-02h`/`I.I.I.I`/`run4theh111z`/`fulcrumassets`) + `The-Cave` (gates `w0r1d_d43m0n`, itself hacked not backdoored, deliberately excluded) + the 9 megacorp servers (each backdoor cuts that company faction's rep requirement 400k→300k). Hardcoded as `TARGET_HOSTS`, which also made the old `isPrivateServer`/`PSERV_PREFIX` skip logic redundant (home/`pserv-*` were never going to be in the allowlist) — removed. Build clean, compiled output confirmed synced by the running watcher; in-game behavior (does it actually skip the excluded hosts on the next pass) not yet separately watched.

**`faction-work-loop.ts` now releases the work slot when the only joined faction has nothing left to buy (`bdec741`).** User report, right after an augment install: only one faction was unlocked with nothing purchasable from it, but the loop kept grinding its rep anyway — `orderFactionsByAugmentGap` ranked every joined faction even when its rep-gap was `Infinity` (NFG folded into the calc now too, instead of being excluded), so a faction with zero payoff still got picked. Fixed by dropping `Infinity`-gap factions from the ranking entirely, and by calling `ns.singularity.stopAction()` in `main()` when nothing's worth targeting but work is still active from a prior tick — Singularity work doesn't stop on its own just because the loop stops renewing it. This lets `controller.ts`'s existing `decideActiveWorkScript` fallback (unchanged, was already correct) actually see "not working" and hand the slot to `company-work-loop.js` for money. Build clean; **not yet confirmed in-game** — watch for `faction-work-loop: switching target X -> (none)` in the tail followed by `controller` launching `company-work-loop.js`.

**`augment-loop.ts` now donates to factions to close rep gaps instead of leaving spare money idle (`c308cad`).** Prompted by the user asking how faction favor works; decided the more actionable win was wiring up `donateToFaction` so money (usually abundant once gang income kicks in) actively closes rep gates that `faction-work-loop.ts` would otherwise grind toward slowly. Each tick, after buying already-affordable augmentations, donates just enough to close the *smallest* rep gap blocking a not-yet-purchasable augmentation (closest-gap priority, mirroring `faction-work-loop.ts`'s own targeting order), skipping the player's own gang faction (the game rejects donations there). Uses the real rep-per-dollar formula (`(amount / 1e6) × faction_rep mult`) but drops the BitNode-multiplier term the same way `hacknet-manager.ts` already tolerates an imprecise Formulas-derived constant elsewhere — the gap is recomputed fresh every tick, so an imprecise guess costs an extra tick, never a wrong outcome. `donateToFaction`'s own boolean return doubles as the 150-favor threshold check, avoiding a second BitNode-gated lookup. **Also fixed an install-ordering bug this surfaced along the way**: `buyCandidates()` runs before the donation, so a donation that closes a rep gate doesn't get spent on that augmentation until the *next* tick's purchase pass — without a fix, a donate-only tick with nothing else purchased and something already queued would trigger `installAugmentations()` immediately, wiping reputation to 0 before the newly-unlocked augmentation was ever bought. Now a successful donation blocks that tick's install too, same pattern as a real purchase. Added `singularity.donateToFaction` (80GB) to `ram-audit`'s cost table. Build clean, synced; **not yet observed live** (needs a real tick to confirm money actually moves and favor/rep track as expected).

**New skill: `ram-costs-refresh`.** Fills the maintenance gap `ram-costs.json`'s own `__note__` called out — re-derive the whole file from upstream `RamCostGenerator.ts` whenever `NetscriptDefinitions.d.ts` is re-fetched for a game-version bump, distinct from `ns-cost-lookup`'s one-function-at-a-time job. Smoke-tested for real: zero drift across every existing entry (core NS, `hacknet.*`, `ui.*`, `formulas.*`, `singularity.*`, `gang.*`, `cloud.*`), and surfaced 19 real `singularity.*` gaps in scripts this repo already runs — including several the two fixes above needed (`getAugmentationPrice`, `installAugmentations`, `purchaseAugmentation`, `checkFactionInvitations`, `getCurrentWork`, `joinFaction`, `stopAction`, `workForFaction`, `getCurrentServer`, `installBackdoor`, and 9 more) — all cross-confirmed against both `RamCostGenerator.ts` and `NetscriptDefinitions.d.ts` and added at the existing conservative outside-BN4/no-SF4 tier. The smoke test caught a real bug in the skill's own Step 4 wording along the way: it didn't tell the reader to respect `ram-costs.json`'s documented "deliberately excludes Corporation API/`dnet.*`" policy, so a literal follow would have proposed bulk-adding namespaces this static estimator can't safely cover (RAM-analyzer collisions there require live `mem`, per `[[bitburner_ram_analyzer_bugs]]`) — fixed before completing the run. This closes the `ram-audit` singularity.* gap tracked in Known Issues since 2026-08-11.

**New skill: `diagnose-loop-bug`.** Formalizes the exact recurring cycle behind the last 6 loop-script fixes in this project's history (`c09d707`, `0d3598f`, `c029546`, `ef74360`, `bdec741`, `c308cad`): a background loop misbehaves because a decision function's gate misses one dimension — found by grepping the function, reading it in full, and checking coverage against every relevant dimension, then patching, build-checking, verifying in-game, and recording a memory note. Chains into the existing `build-check` and `git-commit`/`commit-and-push` skills for the tail. Smoke-tested read-only against `faction-work-loop.ts` (correctly concluded no bug present, rather than manufacturing a false positive) before being shipped.

---

### 2026-08-15: gang power growth via Territory Warfare researched + implemented; two more augment-loop/faction-work-loop fixes confirmed live; post-restart crime fallback fixed

Still BN4 (Singularity). Three commits: one new feature (researched via `/research`), two bug fixes from `diagnose-loop-bug`-pattern reports. No BitNode change.

**Territory warfare power growth implemented (`5252847`).** User asked how faction/territory warfare works and when gang members should start growing power; no prior research existed on this specific mechanic (`[[bitburner_bn2_gang]]` had flagged it "unresolved by sources"). Ran `/research` (10 parallel agents against the game's own TypeScript source — `AllGangs.ts`, `GangMember.ts`, `tasks.ts` — plus community strategy threads). Findings, confirmed from source: clash win chance is a plain ratio `ownPower / (ownPower + rivalPower)`; a member's stat total only counts toward the gang's aggregate power **while assigned the dedicated "Territory Warfare" task** (`baseMoney: 0, baseRespect: 0`); member death only happens during an actual clash, which only occurs once `ns.gang.setTerritoryWarfare(true)` engagement is on — so growing power (task assignment) and risking death (engagement) are independent switches, and grinding power with engagement off is risk-free. A published community automation script engages clashes at a ~2x power ratio (≈67% win chance); the researched consensus range is 60-65%, matching a minority ~90%+ view. **The bug this exposed:** `gang-manager.ts`'s `computeWarfareDecision` already had the 65%/55% engage/disengage hysteresis wired up from an earlier session, but it was dead code — nothing ever assigned any member to the Territory Warfare task, so power could never grow and win chance sat flat forever. **Fix:** `computeTaskAssignments` now dedicates trained members to Territory Warfare once respect is no longer the priority (same cutoff already used to switch from earning respect to earning money), until territory is ~100% secured. Existing 65%/55% thresholds left unchanged — already within the researched-safe range. Findings + fix saved to a new memory note (`[[bitburner_gang_territory_warfare]]`), closing the gap flagged in `[[bitburner_bn2_gang]]` and `[[bitburner_gang_tasks_hacking]]`. Build clean, synced. **Not yet observed live** — needs a real tick to confirm members actually get assigned and gang power/win-chance climbs.

**NFG-gate bug, 4th variant (`1855448`).** User reported the same symptom as the 08-13 "3rd fix" recurring: character grinding Sector-12 rep despite Sector-12 showing "No Augmentations left." Used `diagnose-loop-bug`. Root cause: `canBuyNfg`'s `gatedCount` counter incremented for the player's own gang faction's (`Slum Snakes`) rep-gated augmentations *before* the exclusion check that was supposed to skip them — that exclusion only guarded `donateTarget` selection (confirmed via API docs: `donateToFaction` explicitly rejects the player's own gang faction, and `workForFaction` always fails there too since gang factions offer no work types). Gang rep only grows via separate gang activity, so a gang-locked augmentation sitting rep-gated blocked NFG purchases at *every* faction indefinitely, stranding every tick of rep `faction-work-loop.ts` earned. **Fix:** moved the gang-faction exclusion earlier so it guards `gatedCount` too. **Confirmed live**: character stopped grinding Sector-12 rep, switched to company work (money), then picked faction rep-grinding back up once Daedalus (a real target) appeared — the full intended hand-off chain (faction releases slot → company takes it → faction reclaims for a real target) working end-to-end. `[[bitburner_augment_loop_nfg_gate]]` updated as variant 3 (fixed), closed.

**Post-restart crime fallback fixed (`a7bc3e4`).** User reported that after a fresh post-augment-install restart, the character was idle — no crime, no faction work, no company work. Used `diagnose-loop-bug` again. Two compounding bugs: (1) `controller.ts`'s `decideActiveWorkScript` only ever picked `crime-loop.js` via `!ns.gang.inGang()` — once a gang exists (true for the rest of the playthrough), that path is permanently closed regardless of how fresh the restart is; (2) `crime-loop.ts` was the only one of the three work-loop scripts with a self-stop guard (confirmed via grep — `faction-work-loop.ts`/`company-work-loop.ts` have none), so even fixing (1) alone would've had the script kill itself the instant it launched. **Fix:** `controller.ts` now returns `crime-loop.js` for a 3-minute grace window (`POST_RESTART_CRIME_GRACE_MS`) measured from `CONTROLLER_STARTED_AT` (set fresh on every `controller.js` process start, including the one `installAugmentations`'s `cbScript` triggers) regardless of gang status; `crime-loop.ts`'s self-stop guard removed entirely — `controller.ts`'s kill/run coordinator is now the sole authority, matching its siblings. **Confirmed live**: a follow-up screenshot showing the character working ECorp instead of grinding Daedalus rep turned out to be the fix working as designed, not a bug — Str/Def/Agi were all well above 1 (crime had run briskly post-restart), then company work covered the gap until Daedalus became a real rep-gated target and faction-work-loop reclaimed the slot. `[[bitburner_bn4_singularity]]` updated with this fix.

---

### 2026-08-16: BN4 cleared for real (SF4.1), replay started (BN4.2) — crime-loop's fresh-BitNode-entry RAM race confirmed

No code changes — pure play/BitNode-transition session (`git status` clean throughout, nothing to commit or push).

**BN4 destroyed for real (not flumed) — SF4.1 obtained.** The automation stack confirmed live through 2026-08-15 (gang founding, territory warfare, NFG-gate hand-off chain, post-restart crime fallback) carried the character to a genuine clear. Per `[[bitburner_bitnode_route]]`'s researched order (BN4 must reach SF4.3, not just one clear, or the whole `ns.singularity.*`/`ns.gang.*`-based automation pays a 16x/4x RAM tax outside BN4), the user decided on the spot to replay BN4 two more times before moving to BN6/BN7 — see `[[bitburner_bn4_singularity]]`'s 2026-08-16 update for the full reasoning. Re-entered BN4 immediately (now "BN4.2", 2nd of 3 total clears).

**Crime-loop's post-restart RAM-race fallback (`a7bc3e4`, fixed 2026-08-15) re-verified against a genuine fresh BitNode entry, not just a mid-playthrough augment-install reset.** This mattered because the two reset home RAM differently: an augment install resets stats/money but leaves home RAM/cores upgrades intact, while a true BitNode entry drops home RAM itself back to a bare 32GB — untested territory for whether `crime-loop.js` could still win its RAM race against `controller.js`/`rescan-loop.js`/`home-ram-loop.js`/`home-cores-loop.js` at that much lower starting point. User reported `ps` right after the fresh restart: only the 4 always-resident/unconditional scripts were up (`rescan-loop.js`, `controller.js`, `home-ram-loop.js`, `home-cores-loop.js`) — `crime-loop.js` genuinely didn't fit at 32GB yet, which read as "possibly stuck" until cross-checked against `controller.ts`'s actual gating logic (not just `boot-chain`'s tree, whose RAM figures are known-inflated for singularity-heavy scripts — see `[[bitburner_ram_analyzer_bugs]]`). A second `ps` right after `home-ram-loop.js`'s first purchase (32GB→64GB) showed `crime-loop.js` had joined immediately, alongside `gang-manager.js`/`server-purchase-manager.js`/`program-buy-loop.js`/`hacknet-manager.js` — all four `[gated ≥ 64GB]` scripts crossing the threshold on the same tick. **Conclusion: no bug — the delay was real but self-resolving, exactly the RAM-throttled-retry behavior `controller.ts`'s every-tick launch loop was designed for.** `darknet-manager.js`/`backdoor-loop.js` (the latter ~35.90GB, the priciest of the lower-tier group) were still absent at 64GB — expected, should join as home RAM climbs further. `[[bitburner_bn4_singularity]]` updated with this confirmation.

**Routine housekeeping**: `npm run watch`/`npm run sync` restarted as detached background processes via `dev-watch` (separate short session, no code changes).

**Reframe for the fresh BN4.2 instance**: because a full BitNode entry resets all in-playthrough state (karma, gang, augmentations — but not permanent Source-Files), the 2026-08-15 live confirmations (Territory Warfare task assignment, NFG-gate 4th fix, crime/faction/company hand-off) are validated as *code*, carried over automatically, but the actual *game state* those confirmations describe (a formed gang, growing territory power, NFG purchases unblocked) no longer exists in BN4.2 and needs to play out again from scratch. Treat the "Immediate" goals below as watching this fresh run, not the concluded one.

---

### 2026-08-16 (later): full skill-audit sweep, 3-phase fix-it pass, all pushed

Separate, later session from the BN4.2 replay-start session above — no gameplay/BitNode change, entirely `.claude/skills/` catalog work. User invoked `/skill-audit` cold (fresh context after `/clear`).

**Sweep.** Partitioned all 14 project-local skills into 5 disjoint background sub-agent groups against the standard rubric (bucket fit, deterministic→scripts, templates→assets, re-entered config, AskUserQuestion, Arguments section); every finding verified empirically (ran the scripts, read live source, cross-checked memory notes) before being reported. `diagnose-loop-bug`/`ram-costs-refresh` (both new since the 2026-08-10 audit) got closer scrutiny per the audit's own Step 0. Six skills came back clean: `boot-chain`, `build-check`, `check-unlock`, `dev-watch`, `ns-cost-lookup`, `position-tail-window`.

**Phase 1 (`3631a2c`) — correctness bugs**, all verified against live output before fixing: `activate-check`'s SKILL.md *and* its own script header comment both described the boot chain as `scan-root → controller → hacknet-manager → rescan-loop`; the real topology (confirmed via a live `activate-check.mjs` run) is `scan-root.ts` launching `rescan-loop.ts` and `controller.ts` as siblings, with `controller.ts` separately fanning out to ~a dozen managers including `hacknet-manager.ts`. `new-worker-script` similarly misdescribed `controller.ts`'s dispatch as "a fixed 3-branch weaken/grow/hack if/else" when it's actually a 4-op HWGW batch scheduler. `diagnose-loop-bug`'s Step 4 hot-files list had already drifted from the `feedback-regrep-before-edit-hot-files` memory note it cites (listed `faction-work-loop.ts`, which the note doesn't) — now points at the note live instead of restating a copy. `secret-scan`'s "~250 commits" claim was stale (actual: 147, the script already computes this live). `ram-audit` didn't document that its shape-3 bare-property-collision auto-detection is scoped to the cost table's covered namespaces, so it structurally can't catch a collision in an excluded one — the exact gap that let the `corp-manager.ts`/`hasWarehouse` phantom-charge (2026-08-04) slip past it undetected.

**Phase 2 (`e0160ec`) — cross-cutting de-duplication.** The `LAUNCH_BLOCK` template + `LAUNCH_RETRY_ATTEMPTS`/`DELAY_MS` (5/3000) defaults were still hand-duplicated between `new-background-loop`'s `scaffold-loop.sh` and `reorder-chain-launch`'s SKILL.md Step 4, kept in sync only by "if you change one, change the other" comments — exactly the duplication a 2026-08-10 decision (below) had deliberately scaled back from extracting for real, judging the risk of touching `scaffold-loop.sh`'s corruption-prone sed logic not worth it for what was then "cosmetic drift, not a functional bug." This time: created `new-background-loop/assets/chain-launch-block.ts` as the canonical source, updated both skills' *comments* to point at it (the actual sed-generation logic in `scaffold-loop.sh` was left untouched — only its surrounding comments changed), and verified with a real scratch-clone smoke test — clone, scaffold a test script, confirm the generated wiring diff matches the new asset byte-for-byte, confirm the scratch clone still builds clean. **Real near-miss during this verification**, not git-visible: the first attempt ran `scaffold-loop.sh` via its absolute path from a shell still `cd`'d into the real repo — the script resolves its target root via `git rev-parse --show-toplevel` off the caller's cwd, not its own script path, so it silently wired `darknet-manager.ts` and created `test-loop-scratch.ts` in the **real** repo instead of the scratch clone. Caught immediately via `git status --short` before anything was staged or committed; reverted cleanly (`git checkout --` + `rm`), then re-ran correctly `cd`'d into the scratch clone first. Saved as [[bitburner_scaffold_loop_scratch_test_gotcha]] so a future edit to this script doesn't repeat the near-miss. Also this phase: collapsed `ram-audit`/`ram-costs-refresh`'s independently-retyped excluded-namespace list (which had already drifted — `ram-audit`'s copy omitted `singularity.*` from the "covered" list despite dozens of entries already present) to both point at `ram-costs.json`'s `__note__` instead; gave `ram-costs-refresh`'s Step 6 (a destructive whole-file bulk edit) a real `AskUserQuestion` gate, matching the precedent `ns-cost-lookup` already set for its much smaller single-key adds. Also fixed the same stale boot-chain topology claim in `new-background-loop/SKILL.md`'s own intro, spotted while touching that file (not part of any sub-agent's report — same bug class as `activate-check`'s Phase 1 fix).

**Phase 3 (`37c2725`) — `diagnose-loop-bug` scripts/ + Arguments.** Extracted Step 2's naming-guess-then-fallback grep (already documented as a real wasted-turn Gotcha against `faction-work-loop.ts` from the skill's own 2026-08-13 smoke test) into `scripts/find-decision-function.sh` — runs the `decide*`/`should*`/`canBuy*` guess first, falls back automatically to listing every top-level function instead of relying on the Gotcha being remembered by hand. Verified live against all three real cases: a guess-hit (`controller.ts`'s `decideActiveWorkScript`), and two guess-misses that both correctly fall back (`faction-work-loop.ts`'s `orderFactionsByAugmentGap`, and a **second real miss found during this pass** — none of `augment-loop.ts`'s helper functions match the naming guess either). Added a `## Arguments` section for consistency with sibling skills `check-unlock`/`dev-watch`.

**Deliberately deferred, not fixed:** `new-background-loop`'s `assets/loop-template.ts` bakes the same stale chain-topology text into its `// CHAIN-TAIL` marker comment, which gets copied into every freshly-scaffolded script. Fixing it cleanly requires also touching the paired sed-deletion pattern in `scaffold-loop.sh` that strips the marker's continuation lines by matching specific substrings in their current wording — the same fragile, corruption-prone logic this session was already being deliberately conservative around. The marker is also transient (only visible on whichever script currently happens to be chain tail, gone the next time a script is scaffolded), so the risk/value tradeoff didn't clear the bar this session. Left as a known, low-priority gap — see Known Issues.

Full `npm run build` confirmed clean after all three phases; working tree clean, 3 commits ahead of `origin/main` pending this close-out's push.

---

## Current Goals

**Immediate (next session, top priority) — BN4.2 is done (SF4.2 obtained), BN4.3 (final clear toward SF4.3) is now underway:**
- **Watch `backdoor-loop.ts` actually reach and backdoor `w0r1d_d43m0n` to destroy the BitNode** (`2c41d2f`, 2026-08-19, not yet exercised live) — the mechanism is now wired (added to `TARGET_HOSTS`, same generic install-backdoor-on-rooted-target loop as every other target) but has never fired for real; confirm it triggers the BN4.3 clear once hacking level/rooting catches up, rather than the player needing to notice and backdoor it manually.
- **BN4.2's endgame specifics (territory-warfare 99.5%/99% threshold firing, NFG-donation branch firing, `ef74360`/`c308cad` allowlist/donation confirmation) were never explicitly confirmed before the transition to BN4.3** — carry all of these forward as still-unconfirmed rather than assume they resolved silently; since BN4.3 is a fresh BitNode instance (karma/gang/augments/cash all reset again), re-watch for each from scratch there. See Known Issues for the full list.
- **Confirm the RAM-exclusivity work-loop redesign and `gang-manager.js`'s split architecture hold up across this third full BitNode entry**, not just the snapshots taken during the first two clears.
- **If `crime-loop.ts`'s `focus` flag gets flipped a 3rd time, ask why before just toggling it again** — two reversals with no lasting rationale (`d34cdd8`→`3e17746`, both 2026-08-17/18) suggests the real want might be something else (situational toggle, or suppressing the UI jump without losing focus).

**Short-term:**
- **If `company-work-loop.ts`'s reputation ever looks similarly stuck**, apply the same rotation fix as `faction-work-loop.ts`'s 2026-08-11 change — not done proactively since company reputation doesn't gate anything as impactful as augmentation purchases, but the identical sticky-`targetCompany` pattern is there.
- **Open/optional, user hasn't decided (asked 2026-08-07):** whether to pre-build a `purchaseProgram`-buying script now — moot as of this session, `program-buy-loop.ts` was already built (`661925e`) and covers this.
- Given the BN3 Step 7 livelock's root cause (sequential refresh gates can outlast their own staleness threshold), keep in mind if a similar symptom ever recurs in `corp-manager.ts` once BN3 resumes — the fix so far was longer timers, not a structural resume-from-frontier redesign.
- **If a 3rd/4th variant of the augment-loop/faction-work-loop "gate missing a dimension" bug shape shows up, reach for the new `diagnose-loop-bug` skill** rather than re-deriving the diagnostic method from scratch — it's built exactly from this family's precedent (6 fixes so far).

**Long-term:**
- Clear BN4.3 for real (destroy `w0r1d_d43m0n`, not flume) — the third and final clear for SF4.3, per `[[bitburner_bn4_singularity]]`. `backdoor-loop.ts` should now trigger this automatically once `w0r1d_d43m0n` is rooted and reachable.
- **Resume BN3 corp automation** once back in BN3 — uncomment `corp-manager.js`'s launch block + `computeCorpReserveGb` term in `controller.ts`, per the pause note above. v1 (Agriculture, single division, hands-off) is already complete; v2 backlog (2nd division, R&D, advertising, IPO, faction bribing) is the confirmed brief's out-of-scope list.
- Worth a sanity check of corp funds history when BN3 resumes, given the Step 5 double-dispatch bug (fixed, but ambiguous whether it actually double-charged ~$25B before the fix landed) — low stakes (in-game currency), not blocking.
- Then, per `[[bitburner_bitnode_route]]`'s researched order: BN6/7 next.
- Close every session from here on with `session-closer` — this session itself is another example of the gap this keeps flagging (the BN5→BN4 pivot sat undocumented in `project-state.md` for a full session before this catch-up).

## Recent Decisions

- **Added a diagnostic `ns.print` line and asked the user to paste live tail output rather than guessing a second time** (2026-08-11) — after the first augment-loop fix didn't resolve the reported symptom, the next move wasn't a second speculative patch; it was making the actual decision state (`candidates`/`gated`/`canBuyNfg`/`budget`) directly observable. That single tail snapshot (`candidates=0 gated=60`, $2.9B idle) immediately falsified the "budget starvation" theory and pointed straight at `faction-work-loop.ts`. Same `[[feedback_verify_ingame_before_declaring_fixed]]` discipline as prior sessions, applied one step earlier — before claiming a fix works, not just after.
- **Chose to rotate `faction-work-loop.ts` across all joined factions, not add `donateToFaction`-based rep-buying to `augment-loop.ts`** (2026-08-11, user-directed via `AskUserQuestion`, offered as one of 3 options) — donation was flagged as a smaller/faster stopgap but a dead end for most of the 60 gated augments right now (it's gated per-faction by a favor threshold that itself requires prior reputation, which most factions never had a chance to earn under the old sticky-target design). Rotation fixes the actual root cause instead of working around it.
- **Fixed two `ram-audit`-caught bugs in the new rotation code before it ever reached the game** (2026-08-11) — `Infinity - Infinity` producing `NaN` in the sort comparator (guarded with an explicit equality check before subtracting), and a `??` operator that trips this repo's own confirmed finding that Bitburner's static RAM analyzer sometimes attributes an unrelated ~10GB phantom charge to scripts using nullish-coalescing. Caught by running `ram-audit` as a matter of course after any new script logic, not because either bug was suspected going in.
- **Chose interview + brief + independent-review + Plan-Mode for the BN3 corp scope, rather than jumping straight to code** (2026-08-04) — given the size of the new subsystem (17 new files) and this repo's specific history of RAM-starvation bugs from under-scoped RAM-priority changes, both the brief and the file-by-file plan got an independent second pass (a subagent caught a real blocking gap in the brief; a Plan subagent corrected a real misconception about per-loop-iteration RAM cost) before any code was written.
- **Corp automation supersedes hacking for home RAM via a capped reservation, not a hard on/off gate** (user-directed) — reserve capped at 50% of home RAM so hacking is heavily deprioritized but never starved to literal zero forever, recovering automatically as home RAM grows. Chosen specifically because this repo has a 6-session history of exactly that uncapped-reservation failure shape (see Historical above).
- **Split corp automation into 16 single-purpose worker scripts** rather than fewer, chunkier files — driven by hard RAM-budget math: most `ns.corporation.*` calls cost 10-20GB each, and this repo's static-cost model charges once per *distinct* `ns.*` function referenced (looping the same call over 6 cities in one file is free; referencing 3 different 10GB getters in one file is 30GB). Every worker capped at ≤20GB to stay fundable on a modest fresh-BN3 home RAM budget.
- **Fixed the write-action re-dispatch bug with report invalidation (`ns.rm`), not a longer refresh timer** — once `purchaseWarehouse` was confirmed firing twice for the same cities, the fix needed to guarantee the *next* tick re-verifies real state, not just make staleness less likely. Applied generally via `dispatchWriteAction` so every future write action gets the same protection automatically.
- **Diagnosed the Step 7 stall via added logging and Corporation-UI ground truth, not assumption** — when `corp-manager.ts`'s tail went silent for several minutes, first fixed its total lack of success-path logging (only failures printed before) rather than guessing at the cause; the resulting live dispatch trace pointed straight to the refresh-threshold interaction. Consistent with the standing `[[feedback_verify_ingame_before_declaring_fixed]]` rule.
- **Reused the already-fetched `offices` report for Step 8's morale/energy check instead of adding a separate `STEADY_STATE_REFRESH_CYCLES` tick-counter** (a deviation from the plan's literal sketch) — the file's existing staleness-gated-report mechanism already bounds dispatch frequency the same way, with no new state needed; simpler within the already-approved architecture.
- **Didn't stop at "the UI looks right" for Step 7/9 — traced each claim to a structural code-path or `mem`/`ps`/`free` reading** — e.g. Step 7's confirmation came from `needsSellSetup`'s exact evaluation requirement, not just eyeballing the Sell column; the hacknet-manager.js RAM squeeze was traced with real arithmetic (pre-corp vs. post-corp headroom) rather than assumed to be caused by the just-shipped change. Same `[[feedback_verify_ingame_before_declaring_fixed]]` discipline, applied to a "is this my bug or pre-existing" question rather than a plain pass/fail.
- **Paused BN3 by commenting out corp's launch block, not deleting it** (2026-08-04 night) — the whole 9-step build is done and shouldn't need re-deriving when BN3 is resumed; a one-line uncomment (plus the matching reserve term) is the entire re-enable cost.
- **Gave `gang-manager.js` a real priority gate over `hacknet-manager.js`/`darknet-manager.js`, not just first-in-list attempt order** (2026-08-04 night, user-directed) — attempt-order alone let the two smaller managers launch on RAM the larger one couldn't use yet, then squat on it permanently once resident. Gating their attempt on `gang-manager.js` already running closes that race structurally, at the cost of hacknet/darknet start-up being delayed behind gang whenever RAM is tight (accepted trade-off, matches the user's explicit priority ordering).
- **Split `home-upgrade-loop.ts` into two single-purpose scripts rather than adding a mode flag** (2026-08-06) — a manual run of just RAM or just cores now only pays that one call's RAM cost instead of the combined script's ~99.65GB; matches this repo's existing convention (`hack.js`/`grow.js`/`weaken.js`) of one script per single `ns.*` action rather than a flag-branched multi-purpose file.
- **Reserved home RAM for `scan-root.js`'s transient spawn with the same `isRunning`-gated pattern already used for persistent managers, not a fixed always-on reserve** (2026-08-06) — consistent with every other entry in `currentReserveGb()`, and cheaper: the reserve only actually holds RAM back while `rescan-loop.js` is alive and would be spawning it, not permanently.
- **Kept crime-loop.js/faction-work-loop.js/company-work-loop.js as three separate mutually-exclusive scripts rather than merging them into one** (2026-08-09, user-directed, chosen for RAM) — Bitburner charges RAM once per distinct `ns.*` function *referenced* in a resident script regardless of call count, so a merged script would permanently cost the union of all three's functions (~27.6GB) while running only one at a time costs whichever is active (8.10–11.60GB). The exclusivity logic itself lives in `controller.ts` (`decideActiveWorkScript`) rather than a separate coordinator script, since `controller.ts` already pays its own base cost and already references `ns.isRunning` — the only marginal cost was `ns.kill`+`ns.singularity.getCurrentWork` (+1.0GB total, confirmed live).
- **`augment-loop.ts` only launches once `ns.gang.inGang()`, not just internally gated but at the `controller.ts` launch-attempt level** (2026-08-09) — avoids paying its 28.10GB during the entire pre-gang crime-grinding phase, when it would do nothing but idle anyway.
- **`crime-loop.ts` uses `focus: false` on `commitCrime`, not the API's own `true` default** (2026-08-09, user-reported bug) — `focus: true` forces the game UI to jump to the Work screen on every call, and since crimes are re-committed every few seconds this made the UI unusable. Matches the pre-existing convention `faction-work-loop.ts`/`company-work-loop.ts` already used.
- **Held off on live-eviction (killing a resident lower-priority script to make room) even after two reservation-only fixes proved insufficient** (2026-08-10, user-directed via `AskUserQuestion`) — offered as one of the 4 fix options; user picked shrinking `gang-manager.js`'s actual RAM demand instead of reshuffling who gets starved when. Avoids the thrash risk of a killed script losing in-progress state and repeatedly restarting under sustained RAM pressure.
- **Split `gang-manager.ts` into a cheap orchestrator + `gang-agent-*.ts` workers, mirroring `corp-manager.ts`/`corp-agent-*.ts` exactly** (2026-08-10) — same rationale as the corp split above (RAM charged once per distinct `ns.*` function referenced, regardless of call count): pushing every read into one transient status-snapshot worker and every write into its own tiny action worker means the orchestrator's resident cost is just its own dispatch plumbing (~3.60GB), not the union of everything it might ever decide to do (36.10GB). Decision logic (task assignment, ascension thresholds, equipment budget, warfare hysteresis) is pure computation over the cached report — zero `ns` calls — so it costs nothing to keep resident.
- **Renamed colliding report fields (`hackSkill`, `respectForNextRecruitThreshold`) instead of bracket-notation, for the new `GangStateReport` type** (2026-08-10) — this repo's established fix for the RAM-analyzer's bare-property-name phantom charge (`[[bitburner_ram_analyzer_bugs]]`) is normally bracket notation, since the colliding field names usually belong to the game's own API types that can't be renamed. Here the report type is locally-defined, so renaming is cleaner and avoids scattering bracket-notation workarounds through brand-new code.
- **Scaled back skill-audit Phase 2's "shared chain-launch template" win to syncing the two copies + cross-reference comments, not a real shared asset file** (2026-08-10 late) — extracting one true shared template would mean modifying `scaffold-loop.sh`'s sed-based marker-insertion step, which has a documented history of corrupting files on subtle multi-line-text handling; the actual duplication (`reorder-chain-launch`'s doc example vs. `scaffold-loop.sh`'s generated block) was cosmetic drift (naming/comment only), not a functional bug, so the lower-risk fix was judged sufficient.
- **Left the `resume-session` skill draft in scratchpad rather than installing it into this repo** (2026-08-10 late) — it was scoped as project-agnostic (works off whatever status-doc filenames a repo's own `CLAUDE.md`/`README.md` convention uses), so it wasn't clear yet whether it should land here, in another project, or as a new global dotfiles-managed skill; deferred to the user rather than guessing. **Only lives in a session-scratchpad file today — will be lost if not moved before that scratchpad is cleaned up** (see Known Issues/Next Steps). **RESOLVED 2026-08-11**: now installed as a global skill (`~/.claude/skills/resume-session/`).
- **Put donation logic in `augment-loop.ts`, not `faction-work-loop.ts`** (2026-08-13) — `augment-loop.ts` already tracks money/budget and already computes each faction's rep-gate status, the natural home for spending decisions; `faction-work-loop.ts` has no money awareness. Donates just enough to close the *smallest* rep gap (closest-gap priority, mirroring `faction-work-loop.ts`'s own targeting order) rather than spreading spend across every gap at once.
- **Dropped the BitNode-multiplier term from the donation rep-per-dollar formula** (2026-08-13) — matches `hacknet-manager.ts`'s existing tolerance for an imprecise Formulas-derived constant elsewhere in this repo; since the rep gap is recomputed fresh every tick, an imprecise donation size just costs an extra tick, never a wrong outcome, so the SF5-gated exact multiplier wasn't worth chasing.
- **Used `donateToFaction`'s own boolean return as the 150-favor-threshold check, instead of re-deriving the BitNode-gated favor formula** (2026-08-13) — the call already fails harmlessly (no money moves) below the threshold, so duplicating that logic client-side would be pure risk for no behavior change.
- **A donation that closes a rep gate now blocks that tick's install, same as a real purchase** (2026-08-13, bug caught during review before shipping) — `buyCandidates()` runs before the donation, so without this a donate-only tick with nothing else purchased and something already queued would trigger an immediate install, wiping reputation to 0 before the newly-unlocked augmentation was ever bought.
- **`backdoor-loop.ts`'s target list is a hardcoded allowlist, not a dynamically-derived one** (2026-08-13, user-directed after `/research`) — the qualifying set (5 faction-invite servers + `The-Cave` + 9 megacorp servers) is small, fixed by the game's own design, and cross-checked against 3 independent sources; a hardcoded list is simpler and more auditable than deriving membership from live faction/company data every pass.
- **`faction-work-loop.ts` folds NeuroFlux Governor's own rep gap into `orderFactionsByAugmentGap` instead of special-casing NFG out of the ranking** (2026-08-13) — NFG is repeatable and never truly "owned out," so treating it as just another gap (rather than excluding it or hardcoding it last) keeps the ranking logic uniform and let the fix be a genuine Infinity-only filter rather than a second bespoke branch.
- **`canBuyNfg`'s "nothing real left" check now requires both `candidates.length === 0` AND `gatedCount === 0`** (2026-08-13, 3rd fix on this gate) — `collectCandidates()` filters out rep-gated augmentations, so the first two dimensions checked (owned-real, queued-real) still weren't enough; a real augmentation waiting on faction rep looked identical to "nothing left" without this. The `gatedCount` diagnostic already existed for logging, just wasn't wired into the decision — this is the pattern `diagnose-loop-bug` (built the same session) now formalizes.
- **Built `diagnose-loop-bug` as a project-local Orchestration skill rather than folding the pattern into an existing skill** (2026-08-13, via `skill-suggestion`) — 6 fixes across 2 days (`c09d707`, `0d3598f`, `c029546`, `ef74360`, `bdec741`, `c308cad`) followed the identical shape (grep decision function → read in full → check every dimension → patch → build-check → verify in-game → record memory), clearing the reuse bar for a dedicated skill rather than one-off repetition.
- **Built `ram-costs-refresh` as project-local Data Enrichment, unpinned model, no `scripts/`** (2026-08-13) — bulk-regenerating every `ram-costs.json` entry from `RamCostGenerator.ts` after a game-version bump is judgment work (resolving a nested TS generator source), distinct from `ns-cost-lookup`'s one-function grunt lookup; parsing the generator's nested-object-plus-tier-logic shape was judged too irregular to safely script with grep/regex the way the flatter doc-comment format allows.
- **`ram-costs-refresh`'s gap-detection step must exclude `corporation.*`/`dnet.*` from its "add this" recommendations** (2026-08-13, bug caught during the skill's own smoke test) — `ram-costs.json`'s `__note__` deliberately excludes those namespaces (confirmed Corporation API RAM-analyzer collisions this static estimator can't catch, per `[[bitburner_ram_analyzer_bugs]]`); the skill's first draft would have proposed bulk-adding them anyway, fixed before completing the smoke-test run.
- **Gang power grows only via the "Territory Warfare" task, engagement stays off until strong** (2026-08-15, researched via `/research` against the game's own TypeScript source) — `computeTaskAssignments` now dedicates trained members to Territory Warfare once respect is no longer the priority, until territory is ~100% secured; the existing 65%/55% engage/disengage hysteresis was left unchanged since it already matched the researched-safe 60-65% win-chance range. Growing power this way is risk-free (member death only happens during an actual clash, gated by `ns.gang.setTerritoryWarfare(true)`, a separate switch).
- **`crime-loop.ts`'s self-stop guard removed; `controller.ts` is the sole authority over which work-loop script runs** (2026-08-15) — the guard was the odd one out (`faction-work-loop.ts`/`company-work-loop.ts` have none), and would've killed the script the instant a new restart-grace rule selected it, before it did anything.
- **Post-restart crime fallback is a fixed 3-minute grace window keyed off `CONTROLLER_STARTED_AT`, not a stat threshold** (2026-08-15) — simpler than checking whether stats have already recovered above some value, and `CONTROLLER_STARTED_AT` is set fresh on every `controller.js` process start (including the one an augment install triggers), so it doubles as "time since last fresh restart" for free.
- **Moved the gang-faction exclusion in `augment-loop.ts` earlier so it guards `gatedCount`, not just `donateTarget`** (2026-08-15, 4th fix on this gate) — gang-locked augmentations can never be closed by this automation (can't donate to your own gang, can't work for it either), so counting them toward "is there still something real to wait for" was the uncovered dimension; `donateTarget` already excluded them correctly, `gatedCount` didn't.
- **Replay BN4 two more times before moving to BN6/BN7, rather than banking SF4.1 and moving on** (2026-08-16, per `[[bitburner_bitnode_route]]`'s researched order) — this repo's entire background-automation stack is `ns.singularity.*`/`ns.gang.*`-based, so staying at SF4.1 means every future BitNode pays a 16x RAM tax on it (4x at SF4.2, native at SF4.3). Since the automation is already proven end-to-end from the first clear, each replay is expected to be mostly hands-off rather than a repeat of the original build effort.
- **Revisited and reversed the 2026-08-10 "sync copies + comments, not a real shared asset" call for the `LAUNCH_BLOCK` duplication** (2026-08-16 later) — the original call judged a real shared-asset extraction too risky given `scaffold-loop.sh`'s sed-corruption history. This time, the fix touched only comments in `scaffold-loop.sh` (never its generation logic) and pointed both skills at a new asset file instead — getting the real de-duplication without touching the risky code path, then proved it safe with an actual scratch-clone smoke test rather than falling back to `bash -n`+`npm run build` alone as 2026-08-10 had to.
- **Reported the scratch-clone near-miss and reverted before committing anything, rather than treating a clean final `git status` as sufficient** (2026-08-16 later) — `scaffold-loop.sh`'s smoke test briefly ran against the real repo instead of the scratch clone (cwd-resolution issue, not a script bug); caught immediately via `git status --short`, reverted with `git checkout --`/`rm`, then re-ran correctly scoped. Saved as [[bitburner_scaffold_loop_scratch_test_gotcha]] so the same mistake doesn't cost a repeat near-miss on this specific script.
- **Folded `ram-costs-refresh`'s `AskUserQuestion` fix into the Phase 2 commit instead of holding it for Phase 3** (2026-08-16 later) — it touched the exact same file/section as that phase's excluded-namespace-list fix, so bundling avoided a needless extra Phase-3-only diff to that file.
- **Left `new-background-loop`'s `assets/loop-template.ts` marker-comment topology text unfixed** (2026-08-16 later) — correcting it cleanly also requires changing the sed pattern that strips the marker's continuation lines (which matches specific substrings in the current wording), touching the same corruption-prone logic this session was otherwise careful to avoid, for a comment that's transient and self-clearing on the next scaffold. Judged not worth the risk this pass; see Known Issues.
- **Reverted `crime-loop.ts`'s `focus` flag to `true` for the early-game karma grind specifically, then reverted it back to `false` a day later** (2026-08-17 → 2026-08-18, both user-directed) — the first flip had explicit rationale (pre-gang Mug→Homicide grinding is the first priority right after a fresh BitNode start, so the UI jump is wanted); the second had none stated, treated as the disruption not being worth it after living with it. Memory now flags a 3rd flip as worth asking "why" about rather than just toggling again — two content-free reversals suggest the real want might be a situational toggle, not a fixed default either way.
- **Replaced gang-manager's researched 65%/55% territory-warfare threshold with a user-directed 99.5%/99%** (2026-08-18) — chose to override the "60-65% is enough, wins snowball" aggressive-camp default from the 2026-08-15 research in favor of only clashing at an effectively-guaranteed win. Used 0.995 rather than a literal `=== 1` check since `getChanceToWinClash()` only equals exactly 1 when a rival's power is literally zero, which won't happen while any rival still holds territory (rivals are pre-filtered to territory > 0) — a strict equality check would leave warfare permanently off in practice.
- **`augment-loop.ts` gained a `nfgDonateTarget`, letting `effectiveDonateTarget` fall back to NeuroFlux Governor's own rep gap once nothing real is left to buy or save toward** (2026-08-18, 4th fix on the NFG-gate family) — NFG was previously hard-excluded from ever being a donation target at all, so a multi-trillion-dollar idle budget could only close its rep gap via the slow work-grind path. Used an explicit `!== null` ternary instead of `??`, per this repo's confirmed RAM-analyzer phantom-charge finding for nullish-coalescing.
- **Excluded NeuroFlux Governor from `faction-work-loop.ts`'s `orderFactionsByAugmentGap` entirely, rather than relying on the donation fix above to make the grind worthwhile** (2026-08-18, 5th fix on the NFG-gate family, and the actual root cause of the recurring "grinding Sector-12 for NFG" symptom) — NFG's requirement renews every level, so keeping its gap in the ranking meant a faction with zero *real* augmentations left never dropped out of contention and stayed the permanent (and, when favor is too low to donate, completely dead-end) work target. Matches the exclusion `augment-loop.ts`'s own `collectCandidates` already had. Confirmed live: Sector-12 rep went flat, loop picked up BitRunners (3 real augmentations left) instead.
- **Added `w0r1d_d43m0n` to `backdoor-loop.ts`'s `TARGET_HOSTS` instead of building a dedicated destroy-BitNode script or calling `destroyW0r1dD43m0n()` directly** (2026-08-19) — the existing generic install-backdoor-on-rooted-target loop already does exactly what's needed once the target is in its allowlist, and the game's own doc confirms backdooring `w0r1d_d43m0n` (not `ns.hack()`, which the removed comment had claimed) is the hacking-route way to destroy the BitNode, cheaper than the explicit API call. Deliberately leaves the player on the BitVerse selection screen rather than auto-fluming to a specific next BN (`installBackdoor` has no `nextBN` param) — picking the next BitNode stays a manual choice per the researched route in `[[bitburner_bitnode_route]]`.
- **Diagnosed the `/dev-watch` "sync running but game won't connect" report as a stale-PID false positive, not a config issue** (2026-08-17) — `dev-watch.sh status`'s `kill -0` check only proves *some* process holds the recorded PID, not that it's still the original one; the real `bitburner-filesync` had died and the OS recycled its PID onto `tsc -w`'s own child. Fixed with a clean stop/start rather than touching any config; not yet hardened at the script level (status check doesn't verify the PID's command name) — see `[[bitburner_devwatch_pid_reuse]]`.
- **Scaffolded `loop-bug-investigator` (project-local) and `transcript-scanner` (global, NixOS-managed) custom agents from a corpus-wide log-mining pass, rather than leaving each skill to keep re-deriving its own transcript-mining logic** (2026-08-17) — four skills (`session-closer`, `skill-suggestion`, `skill-upgrade`, `skill-audit`) each independently hand transcript-scanning work to ad-hoc `general-purpose` agents; `transcript-scanner` centralizes the interpretation layer (this project's transcript format + the "never read whole files" doctrine) so all four (and future callers) can fan out to it directly. `loop-bug-investigator` is the read-only fan-out unit for `diagnose-loop-bug` when 2+ loops misbehave at once, justified by the augment-loop/faction-work-loop NFG family alone hitting 5 variants by this session.

## Known Issues / Tech Debt

- **RESOLVED 2026-08-18**: whether `faction-work-loop.ts`'s work-target ranking still had a hidden dead-end after the NFG-gate family's 4th fix — it did (`24d82d4`, NFG's own gap was never excluded from `orderFactionsByAugmentGap`, so a zero-real-augs faction like Sector-12 stayed the permanent target). Confirmed live: Sector-12 rep flat, BitRunners picked up instead, money grew $694.865b → $2.079t in under an hour. `[[bitburner_faction_work_loop_stopaction]]` and `[[bitburner_augment_loop_nfg_gate]]` both updated (variant 5 / cross-reference).
- **NEVER CONFIRMED LIVE IN BN4.2, NOW CARRIED INTO BN4.3**: gang-manager's 99.5%/99% territory-warfare engage threshold (`e0a8ce3`), `augment-loop.ts`'s NFG-donation branch (`6dca5e6`), gang Territory Warfare task assignment (`5252847`), `ef74360` (backdoor-loop allowlist), `c308cad` (augment-loop donations, pre-NFG version). BN4.2 was confirmed complete by the user (2026-08-19) without any of these being explicitly re-observed — all are code-confirmed only. Since BN4.3 is a fresh BitNode instance, each needs fresh live observation there rather than being assumed carried-over-working.
- **NOT YET EXERCISED LIVE**: `backdoor-loop.ts`'s new `w0r1d_d43m0n` target (`2c41d2f`, 2026-08-19) — build-checked and synced, but no `w0r1d_d43m0n` was reachable this session to actually confirm the backdoor call fires and destroys the BitNode as the game's docs describe. First real test will be whenever BN4.3 reaches it.
- **`dev-watch.sh status`'s liveness check trusts `kill -0` alone, so it can false-positive "running" after PID reuse** (found live 2026-08-17, see `[[bitburner_devwatch_pid_reuse]]`) — worked around by a clean stop/start, not yet hardened (e.g. matching the recorded PID's command name). Low priority unless it recurs.
- **RESOLVED 2026-08-16**: whether `crime-loop.ts`'s post-restart RAM-race fallback (`a7bc3e4`) also holds on a genuine fresh BitNode entry (home RAM reset to 32GB), not just a mid-playthrough augment-install reset (RAM/cores intact) — confirmed self-resolving via two `ps` snapshots around `home-ram-loop.js`'s first purchase. See the 2026-08-16 section above and `[[bitburner_bn4_singularity]]`.
- **RESOLVED 2026-08-11 → 2026-08-15**: the faction-rotation fix (`0d3598f`) itself held up — reputation genuinely accumulated across factions — but the NFG-only *symptom* needed two more independent gate-dimension fixes (`c029546` rep-gating, then `1855448` gang-faction reachability) before it actually cleared. Both confirmed live 2026-08-15, alongside `bdec741` (faction-work-loop work-slot release). `[[bitburner_augment_loop_nfg_gate]]` closed as variant 3.
- **RESOLVED 2026-08-15**: post-restart idle bug (`a7bc3e4`) — `decideActiveWorkScript` couldn't select crime post-gang, and `crime-loop.ts`'s self-stop guard would've killed it anyway. Fixed with a 3-minute post-restart grace window plus removing the guard; confirmed live (crime ran briefly post-restart, then company/faction handoff worked as designed).
- **RESOLVED 2026-08-13**: `ram-audit`'s missing `ns.singularity.*` costs (was: `purchaseAugmentation`, `installAugmentations`, `getAugmentationPrice`, `checkFactionInvitations`, `getCurrentWork`, `joinFaction`, `workForFaction` showing `! unknown cost`) — the new `ram-costs-refresh` skill found and closed 19 gaps in this namespace, including all of these, cross-confirmed against both `RamCostGenerator.ts` and `NetscriptDefinitions.d.ts`.
- **RESOLVED 2026-08-10** (was: `gang-manager.js`-vs-`hacknet-manager.js`/`darknet-manager.js` RAM squeeze — real priority fix live, long-term durability not yet proven unattended). Root cause fixed by shrinking `gang-manager.js` from 36.10GB to ~3.60GB (see the 2026-08-10 section above), not just re-prioritizing it — confirmed live via `mem` and `ps` the same session. `[[bitburner_corp_hacknet_ram_squeeze]]` is superseded by `[[bitburner_gang_manager_ram_split]]`.
- **`scan-root.js` RAM-starvation fix (2026-08-06, `67f3328`) — confirmed fixed in-session, same "not yet proven across a cold restart" caveat as the gang-manager fix above.** `[[bitburner_scan_root_ram_starvation]]`: user watched `tail rescan-loop.js` go from constant "Cannot run scripts/scan-root.js" spam to clean after the fix synced, same session. Only a live warm-fix verification, not a full `killall` + cold-restart re-check yet.
- **Step 8's `buyTea`/`throwParty` dispatch itself is untested, and now dormant** — corp is paused (BN5 pivot), so this can't be exercised until BN3 resumes. Energy/morale were at 100% (above threshold) both times checked before the pause, so neither worker had fired yet even while corp was live. Spot-check after real production time once BN3 is resumed.
- **Ambiguous whether the Step 5 double-dispatch bug actually double-charged ~$25B** before the fix landed — `purchaseWarehouse` fired twice per city with no caught error either time. Low-stakes (in-game currency), never confirmed either way.
- **`ram-audit`'s cost table excludes the entire Corporation API** (confirmed via `.claude/skills/ram-audit/SKILL.md`, reconfirmed by `ram-costs-refresh`'s smoke test 2026-08-13) — it can't auto-detect the bare-property-name RAM-analyzer collision for any `corp-agent-*.ts`/`corp-manager.ts` file the way it does for gang/hacking scripts. Live `mem` is the only reliable check for this file family, whenever BN3 resumes.
- (Historical BN4-era tech debt — `MEANINGFUL_PREP_THREADS`/`fragmentationSlopGb` tuning, `omega-net` dead-weight, RAM-analyzer trigger-list completeness — specific to the now-inactive BN4 hacking saga from the first BN4 visit; dropped from active tracking, recoverable from git history if needed.)
- **RAM-exclusivity redesign for the work-loop group is only confirmed via one live snapshot** (`ps`+`mem`+`tail`, 2026-08-09, right after the fix shipped) — not yet watched across an actual faction→company fallback transition, a company→faction re-probe, or a cold restart.
- **RESOLVED**: global skill fixes from the 2026-08-10-late skill-suggestion/skill-upgrade run (`find-last-skill-invocation.sh`'s slash-command-detection fix, `skill-upgrade`'s Step 2 project-local-first check) are confirmed live in `~/.claude` — this session's own transcript scan exercised the fixed detector directly.
- **RESOLVED 2026-08-16 (later)**: the 2026-08-10 gap where `scaffold-loop.sh` edits couldn't be live-tested end-to-end (a `git clone` permission denial blocked the scratch-clone test that session) — this session's Phase 2 edit to the same script *was* verified via a real scratch-clone-and-scaffold run (clone succeeded once not combined with `rm -rf` in one command; generated wiring diff matched the new shared asset byte-for-byte; scratch clone built clean).
- **`new-background-loop`'s `assets/loop-template.ts` still bakes the pre-2026-08-16-Phase-1 stale boot-chain topology into its `// CHAIN-TAIL` marker comment** — every freshly-scaffolded script briefly carries "scan-root.ts -> controller.ts -> hacknet-manager.ts -> ... -> this one" until the next scaffold call moves the marker elsewhere. Deliberately left unfixed 2026-08-16 (later) since a clean fix also requires changing `scaffold-loop.sh`'s sed continuation-line-deletion pattern (matches specific substrings in the current wording) — touching the same corruption-prone logic this session was otherwise careful to avoid, for a transient, self-clearing comment. Low priority; fix alongside the next `scaffold-loop.sh` change that's already touching that logic for another reason, rather than as a standalone edit.
- **`scan-session.sh transcript`'s per-file `jq` extraction loop can silently stop partway through a large multi-transcript batch** (observed 2026-08-13, this session-closer run) — asked to scan 12 transcripts since the last close (several 300KB-1MB+), the saved output only ever covered the first 2 before truncating with no error; the other 10 (including the ones covering all 4 of today's game-code fixes) had to be re-scanned by hand via targeted `jq`/`grep` calls. Likely a Bash-tool output-size/time limit on the single combined command rather than a script bug — worth batching `scan-session.sh transcript` calls (e.g. a handful of files per invocation) instead of one call across a whole multi-session gap, or flagging this as a `session-closer` Gotcha if it recurs.

## Next Steps

- **BN4.2 is complete (SF4.2 obtained); BN4.3 — the third and final clear toward SF4.3 — is now underway.** Watch `backdoor-loop.ts` actually trigger the BitNode destroy via `w0r1d_d43m0n` (new this session, unexercised); re-watch every item in Known Issues above from scratch in this fresh instance (territory-warfare threshold, NFG-donation branch, backdoor-loop allowlist, pre-NFG augment donations) since none were confirmed before BN4.2 ended.
- Watch `gang-manager.js`'s split architecture hold up over this third full BitNode entry — confirm respect/members climb again, not just that the process is resident.
- After BN4.3 lands (SF4.3, native 1x Singularity RAM cost everywhere), per `[[bitburner_bitnode_route]]`'s order: BN6/7 next; BN3 corp resume is still pending a return to BN3.
- Next program unlock → run `check-unlock`.
- New `ns.*` RAM cost needed → run `ns-cost-lookup` for a single function, or `ram-costs-refresh` after a game-version bump to re-derive the whole table (and cross-check against live `mem` for Corporation API functions specifically — deliberately out of scope for both).
- If a loop script misbehaves again, reach for the new `diagnose-loop-bug` skill (its Step 2 now runs `scripts/find-decision-function.sh` automatically) rather than re-deriving the diagnosis method.
- Close every session with `session-closer` going forward.
- Low-priority: fold a fix for `new-background-loop/assets/loop-template.ts`'s stale `// CHAIN-TAIL` marker topology text into the next `scaffold-loop.sh` change that's already touching its sed continuation-line logic for another reason (see Known Issues) — not worth a standalone edit to that script.
